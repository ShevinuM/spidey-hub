# Phase 03 — profile

> **Status: approved for execution 2026-09-08. Phases 01–02 closed.** Binding decisions: `Instructions/00-phases.md`. **D21 is already settled** — phase 02 ran first and recorded the snapshot-path mechanics in `Instructions/01-pre-phase/recipe-feature-map.md` (§ "D21 snapshot-path decision"). This phase REUSES that shape verbatim (architecture R003); it does not re-derive it.

## Context

`src/features/profile`. Declared out-of-context wiring: `profile`/`profile-visual` Playwright project entries; legacy-tree import fallout; `content.config.ts` collection pointer if `content/profile` moves (D22).

## Objective

The smallest feature migrated end-to-end — proving the per-feature loop: specs ported → source moved → full gate green → goldens byte-identical.

## Background — move table

| v1 | v2 |
|---|---|
| `src/components/Profile.svelte` | `src/features/profile/components/Profile.svelte` (flat, files-and-naming R012 — no forced folder split) |
| `src/lib/net.ts` | `src/features/profile/lib/net.ts` |
| `src/lib/resume.ts` | `src/common/lib/resume.ts` **only if** grep confirms ≥2 consumers (profile + cmdline, D16); else profile's `lib/` |
| `src/content/profile/` | `src/features/profile/content/` + `content.config.ts` pointer update |
| spec `profile.spec.ts`; unit `net.test.ts` | `src/features/profile/tests/ui/e2e/`; `tests/unit/` |
| profile-mapped goldens (per recipe map) | `src/features/profile/tests/ui/visual/goldens/<viewport>/` |

## Steps (the per-feature loop — never fuse steps)

- [x] **1. Port specs + goldens.** Spec verbatim (import paths only; strip `.ts` extensions per D23(d)/FX.1). Seed goldens byte-identical (`cp`, then `cmp` each against v1). Create the visual spec from the v1 pipeline slice (D5). **Reuse D21 verbatim** as recorded in `Instructions/01-pre-phase/recipe-feature-map.md`: per-project `snapshotPathTemplate` with the viewport hardcoded from the `viewports` array entry (never `{projectName}`), goldens at `src/features/profile/tests/ui/visual/goldens/<viewport>/` (feature tests nest under `src/features/<f>/tests/`, unlike common's top-level `common/tests/`). Add `profile`/`profile-visual-<viewport>` project entries; shrink `legacy` (filter profile-owned recipes out of the legacy identical.spec via the same set-filter mechanism phase 02 used).
  *Verify (D20):* real build → `--project=profile` + `--project=legacy` green against un-moved source; fixture build → `--project=profile-visual` green; `cmp` clean.
  *Result:* done. `tests/e2e/profile.spec.ts` moved (not copied) to `src/features/profile/tests/ui/e2e/profile.spec.ts`, import fixed to `"../../../../../../common/tests/ui/support/fixtures"` (extensionless, D23(d)/FX.1 — 6 levels up from the new nested location, verified with `node -e "path.relative(...)"`). Goldens seeded via `cp` to `src/features/profile/tests/ui/visual/goldens/{1512x945,1920x1080}/07-profile.png`, `cmp`-verified byte-identical against v1's copies at `/Users/shev/Development/spidey-hub`, then the now-orphaned originals removed from `tests/visual/goldens/`. New `src/features/profile/tests/ui/visual/identical.spec.ts` created modeled on `common/tests/ui/visual/identical.spec.ts`'s exact shape (D21(a) reused verbatim, only the path prefix differs — nests under `src/features/profile/tests/` per the map file's recorded convention), filtering `recipes` to `PROFILE_OWNED_RECIPE_NAMES = {"07-profile"}` (07-profile lives in the plain `recipes` array, confirmed by grep). `tests/visual/identical.spec.ts`'s exclusion set renamed `COMMON_OWNED_RECIPE_NAMES` → `SPLIT_OWNED_RECIPE_NAMES` (truthful rename now that more than one context's recipes are filtered there) and `"07-profile"` added to it. Added `profile-<viewport>`/`profile-visual-<viewport>` project entries to `playwright.config.ts` (own `snapshotPathTemplate`, viewport hardcoded, `src/features/profile/` prefix — not `{projectName}`). `package.json`: `test:e2e` gained `--project=profile-1512x945 --project=profile-1920x1080`; `test:visual` gained the new spec's path.
  Verified against **un-moved source** (Profile.svelte/net.ts/resume.ts/content still at v1 paths): real build (`pnpm build`) succeeded; `E2E_EXPECT_FIXTURES=0 playwright test --project=smoke --project=legacy-1512x945 --project=legacy-1920x1080 --project=profile-1512x945 --project=profile-1920x1080` → **438/438 passed**. Fixture build (`pnpm build:fixtures`) succeeded; `E2E_EXPECT_FIXTURES=1 playwright test tests/visual/identical.spec.ts tests/visual/adversarial-fixtures.spec.ts common/tests/ui/visual/identical.spec.ts src/features/profile/tests/ui/visual/identical.spec.ts` → **50/50 passed** (17 legacy-owned recipes + 3 adversarial + 4 common-owned + 1 profile-owned, ×2 viewports where applicable, boot recipes once each). `public/generated/{grep-index,fs-index}.json` regenerated as a side effect of the builds; diffed programmatically (not just `git diff`) — only the expected spec-file path rename (`tests/e2e/profile.spec.ts` → the two new `src/features/profile/tests/ui/...` paths), nothing else. Unrelated live-data drift in `public/generated/contributions.json`/`src/generated/commits/daily-tech-digest.json`/`src/generated/file-icons.json` (network-fetched, day-to-day noise) reverted with `git checkout --` before committing, to keep the commit scoped.
- [x] **2. Move source** per table (D15/D16); update imports + any literal-path assertions (D8); delete originals.
  *Verify:* `test ! -e src/components/Profile.svelte`; `grep -rn "lib/net\b\|components/Profile" src tests` shows only new paths.
  *Result:* done, via `git mv`: `Profile.svelte`→`src/features/profile/components/Profile.svelte`; `net.ts`→`src/features/profile/lib/net.ts`; `content/profile/`→`src/features/profile/content/`. **`resume.ts`→`src/common/lib/resume.ts`** (D16): grepped real consumers first (not comment mentions) — exactly 2, `Profile.svelte`'s `downloadResume` import and `src/bootstrap/terminalState.svelte.ts`'s `case "resume": downloadResume()` (the `:resume`/`:cv` cmdline dispatch D16 anticipated as "cmdline" — it actually lives in bootstrap's terminalState, not a separate Cmdline.svelte file, but is the same dispatch-from-anywhere consumer the map file meant). ≥2 real consumers in different contexts confirmed → promoted to `common/lib/`, satisfying the rule-of-three.
  Imports fixed: `Profile.svelte` (`ProfileData`/`downloadResume`/`Meter` → `../../../common/lib/{data,resume}` / `../../../common/lib/../components/Meter.svelte`, verified via `path.relative`); `terminalState.svelte.ts` (`../lib/resume` → `../common/lib/resume`); `content.config.ts`'s `profile` collection `base` → `src/features/profile/content` (D22-style pointer update, out-of-context wiring declared in Context); `src/common/lib/data.ts`'s stale header-comment path reference updated for accuracy (mechanical, one line).
  **Two common→feature edges, mechanical-only per this phase's scope, flagged for the auditor:** `src/common/components/Meter.svelte` (net.ts's only source consumer) now imports `../../features/profile/lib/net` directly, and `src/common/components/PaneTree.svelte` (already flagged by phase 02's own audit for importing 6 then-transient feature components) now imports `../../features/profile/components/Profile.svelte` — a real bounded context, not the transient `src/components/` legacy tree D23(c) literally names. Both are R004 violations (`common/` depending on a feature) that pre-existed structurally (phase 02 already noted PaneTree's edge) and are unavoidable while `PaneTree`/`Meter` render feature components directly and no bootstrap-level registry/injection exists yet — introducing one now would be an uninstructed architecture redesign (R008's composition mechanism), out of this phase's scope (`src/features/profile` + declared wiring only). Only the import **path** was updated (move, don't rewrite); the dependency direction is unchanged from before the move. This needs an orchestrator ruling at audit time (same channel phase 02 used for PaneTree): either extend D23(c) to cover "a common component reaching into any feature that still lacks the bootstrap-injection mechanism" until phase 11, or accept as a tracked/deferred item.
  D8 literal-path assertion: `common/tests/unit/views.test.ts:34`'s `expectedView()` `path === "src/components/Profile.svelte"` → `"src/features/profile/components/Profile.svelte"` (the `grepPathToView()` regex itself is basename-anchored, `/(^|\/)Profile\.svelte$/`, so it needed no change). `tests/unit/net.test.ts`'s import fixed in this step (`../../src/features/profile/lib/net`), in place — the file itself moves in step 5, per the plan's step ordering, so the unit gate stays green now rather than going red until step 5.
  `pnpm generate` re-run after the move; `public/generated/{grep-index,fs-index}.json` diffed programmatically — only the 4 moved paths changed (`src/components/Profile.svelte`, `src/content/profile/profile.md`, `src/lib/net.ts`, `src/lib/resume.ts` → their new homes), nothing else. Unrelated live-data drift (`contributions.json`/`commits/*.json`/`file-icons.json`) reverted before committing, same as step 1.
  Old-path verify: `test ! -e src/components/Profile.svelte` / `src/lib/net.ts` / `src/lib/resume.ts` / `src/content/profile` all pass; `grep -rn "lib/net\b|components/Profile\b" src tests` shows only the two updated common-component imports and the two updated unit-test lines above — no stale old-path residue.
- [x] **3. Full gate.** check + lint + unit + D20 both invocations, all projects.
  *Result:* re-run from the clean, fully-committed tree (`git status` empty apart from the pre-existing untracked `.claude/`) after step 2's commit. `pnpm check` (astro check + tsc + svelte-check): 0 errors, 10 pre-existing warnings (unrelated `z.string()` deprecation hints/`await`-has-no-effect, not touched this phase), 949 files, 0 errors, 4 files-with-problems (pre-existing). `oxlint`: exit 0, no findings. `pnpm test:unit`: 339/339 passed, 19/19 files. D20 real-build invocation (`smoke`+`legacy-1512x945`+`legacy-1920x1080`+`common-1512x945`+`common-1920x1080`+`profile-1512x945`+`profile-1920x1080`): **1012/1012 passed**. D20 fixture-build invocation (`tests/visual/identical.spec.ts`+`adversarial-fixtures.spec.ts`+`common/tests/ui/visual/identical.spec.ts`+`src/features/profile/tests/ui/visual/identical.spec.ts`): **50/50 passed**. `public/generated/{grep-index,fs-index}.json` unchanged by this step (already regenerated/committed in step 2); unrelated live-data drift from re-running the builds (`contributions.json`/`commits/daily-tech-digest.json`/`file-icons.json`) reverted before reporting, consistent with steps 1-2.
- [x] **4. Golden parity.** Zero churn (`diff -rq` all goldens vs v1 slices).
  *Result:* `diff -rq tests/visual/goldens <v1>/tests/visual/goldens` reports only the 5 files that legitimately left this tree per viewport ("Only in v1": `06-editor.png`/`15-cmdline.png`/`18-split.png`/`19-choose-tree.png` from phase 02, plus `07-profile.png` from this phase) — zero `differ` lines. `cmp`-verified byte-identical: `07-profile.png` (both viewports, at its new `src/features/profile/tests/ui/visual/goldens/` home) and spot-checked `06-editor.png` (common's home, unaffected by this phase). Total PNG count unchanged: 42 before and after (21 recipes × 2 viewports), just redistributed across `tests/visual/goldens/` (17 remaining) + `common/tests/ui/visual/goldens/` (4) + `src/features/profile/tests/ui/visual/goldens/` (1, this phase).
- [x] **5. Move unit tests** → `src/features/profile/tests/unit/`. *Verify:* `pnpm test:unit` green, count unchanged.
  *Result:* `git mv tests/unit/net.test.ts src/features/profile/tests/unit/net.test.ts`; import path fixed to `../../lib/net` (one level shallower than the interim `../../src/features/profile/lib/net` used since step 2, now that the test file lives inside the feature folder itself); a stale header-comment cross-reference to `tests/e2e/profile.spec.ts` also updated to its new location. No `vitest.config.ts` change needed — phase 02 already forward-declared the `src/features/*/tests/unit/**/*.test.ts` glob entry for this. `pnpm test:unit`: 339/339 passed, 19/19 files — identical count to before the move (pure relocation).
- [x] **6. Feature harness (D24) — this phase SETTLES the mechanism for 04–11.** Build the fixture-build-only harness route (preferred: `src/pages/harness/[feature].astro`, `getStaticPaths` → `[]` unless `PORTFOLIO_FIXTURES=1`) mounting `Profile` with seeded fixture props; specs in `src/features/profile/tests/ui/harness/` under a `profile-harness` project (fixture build); record the mechanism in `Instructions/01-pre-phase/recipe-feature-map.md` alongside D21's shape, and add the checklist entries per D24(c) in the same change.
  *Verify:* fixture build → `--project=profile-harness` green; real build → `dist/` contains no `harness/` output; checklist entries present.
  *Result:* done, full mechanism recorded in `Instructions/01-pre-phase/recipe-feature-map.md`'s new "D24 feature harness mechanism" section (alongside D21's). Built `src/pages/harness/[feature].astro`: `getStaticPaths` returns `[]` unless `PORTFOLIO_FIXTURES==="1"`, else `[{params:{feature:"profile"}}]`; imports `bootstrap/Layout.astro` (global CSS/mobile-block chrome) directly but NOT `Terminal.svelte` (the kernel this route exists to exclude); mounts `features/profile/components/Profile.svelte` with `client:load` and fixture props `buildProfile((await getCollection("profile"))[0])` (profile's collection isn't fixture-switched per D7, so no extra handling needed) via a small explicit per-feature switch in the frontmatter/template (not a registry — D1's context split isn't complete until phase 11, so nothing generic to discover from yet; each later phase appends its own branch here). This makes the page import a feature component directly, which reads as breaking D10's "pages import only from bootstrap" — recorded as a **declared exception** per D24's own wording, not a violation.
  Harness spec: `src/features/profile/tests/ui/harness/profile.spec.ts` (3 tests) — mount + dossier/CV-link/contact-row content, the SIGNAL meter's own independent rAF loop, and absence of kernel chrome (`status-bar-windows` count 0). Deliberately does NOT assert the `r` hotkey — `Profile.svelte`'s `handleKey()` export is only ever invoked by `Terminal.svelte`'s own keydown delegation, which doesn't exist in the harness.
  `playwright.config.ts`: added `profile-harness` project (`testDir: src/features/profile/tests/ui/harness`, one project at `viewports[0]` only, no per-viewport pair — functional-only, no goldens). `package.json`'s `test:visual` gained the harness spec's path (fixture-build-only, alongside the other `*-visual` specs — same file-path-based project selection already used there).
  Checklist entries (D24(c)) added in this same change: `docs/checklist/testing/README.md` new **R011** (harness spec/route placement) and `docs/checklist/testing/e2e-testing.md` new **R018** under a new "Feature harness specs" section (what a harness spec may/may not assert).
  Verified: `pnpm check` 0 errors (114 files, up from 112 — the new `.astro`/spec files type-check clean). Real build (`pnpm build`, dist wiped first): `find dist -iname "*harness*"` empty — confirmed `dist/harness` absent. Fixture build (`pnpm build:fixtures`, dist wiped first): `dist/harness/profile/index.html` present. `E2E_EXPECT_FIXTURES=1 playwright test .../identical.spec.ts ×4 + src/features/profile/tests/ui/harness/profile.spec.ts`: **53/53 passed** (the prior 50 + 3 new harness tests, `profile-harness` project). Re-ran the D20 real-build invocation once more from this final state (`pnpm build` + all 7 e2e projects): **1012/1012 passed** — the new harness page/route doesn't affect the real build's existing routes. `public/generated/{grep-index,fs-index}.json` re-diffed programmatically: only the 3 new harness-related paths added (the `[feature].astro` route, its spec, plus `net.test.ts`'s step-5 move finally showing up in this regen), nothing else; unrelated live-data drift reverted again before committing.

## Acceptance criteria

Loop verified 1–6; verifier PASS + auditor clean on `src/features/profile` (D23 exemptions); commit per 00-phases.md. Stop: no POM refactor, no behavior change, no golden regeneration.

## Results

All 6 steps complete and green at the final commit; see each step's own *Result* line above for detail. Summary for the orchestrator:

**Commits (local, `frontend-rewrite`, in order):**
1. `6f712ea` — step 1: port `profile.spec.ts` + goldens + new visual spec/projects to `src/features/profile`.
2. `8aa41ad` — step 2: move `Profile.svelte`/`net.ts`/`resume.ts`/profile content to their v2 homes.
3. `2f89e11` — step 3: record the full-gate re-verification.
4. `f5e8100` — step 4: record golden-parity verification.
5. `c77777b` — step 5: move `net.test.ts` to `src/features/profile/tests/unit`.
6. `5f6bf80` — step 6: add the `profile-harness` route/spec/project, settling D24's mechanism.

**D21 shape reused (not re-derived):** `profile-visual-<viewport>` projects each carry their own `snapshotPathTemplate` with the viewport hardcoded as a literal (never `{projectName}`), goldens at `src/features/profile/tests/ui/visual/goldens/<viewport>/` — same shape phase 02 settled, only the path prefix differs since feature tests nest under `src/features/<f>/tests/` (map file already anticipated this).

**D16 rule-of-three verdict:** `resume.ts` → `src/common/lib/resume.ts`. Grepped real consumers (not comment mentions): exactly 2 — `Profile.svelte`'s `downloadResume` import and `src/bootstrap/terminalState.svelte.ts`'s `case "resume": downloadResume()` (the `:resume`/`:cv` cmdline dispatch the map file's D16 note anticipated as a second "cmdline" consumer — it lives in bootstrap's terminalState, not a separate `Cmdline.svelte`, but is the same behavior). ≥2 consumers in different contexts → promoted to `common/lib/`, matching the map file's own prediction.

**D24 harness mechanism settled** (full record in `Instructions/01-pre-phase/recipe-feature-map.md`'s new "D24 feature harness mechanism" section): one shared dynamic route `src/pages/harness/[feature].astro`, `getStaticPaths` → `[]` unless `PORTFOLIO_FIXTURES=1`; imports `bootstrap/Layout.astro` but not `Terminal.svelte`; mounts the feature's own top-level component directly with `client:load` and fixture props via a small per-feature switch (not a registry — none is possible until phase 11); harness specs in `<feature>/tests/ui/harness/` under a `<feature>-harness` project (fixture build, one project at the primary viewport, no goldens). Checklist entries landed in the harness commit: `testing/README.md` R011 (placement), `e2e-testing.md` R018 (what a harness spec may/may not assert).

**Two common→feature edges, mechanical-only, flagged for the auditor (same channel phase 02 used for its own PaneTree finding):**
- `src/common/components/Meter.svelte` now imports `../../features/profile/lib/net` directly (net.ts's only consumer).
- `src/common/components/PaneTree.svelte` (already flagged by phase 02's audit for importing then-transient feature components) now imports `../../features/profile/components/Profile.svelte` — a real bounded context, not the transient `src/components/` tree D23(c) literally names.
Both are R004 violations (`common/` depending on a feature) that were structurally inevitable the moment any single feature actually left the legacy tree while `PaneTree`/`Meter` still render feature components directly with no bootstrap-level injection mechanism — introducing one now would be an uninstructed R008 redesign, out of this phase's scope. Only import paths were updated (move, don't rewrite); dependency direction is unchanged from before the move. Needs an orchestrator ruling at audit time: extend D23(c) to cover "a common component reaching into any feature that still lacks the bootstrap-injection mechanism, until phase 11 completes the split" (recommended, since this exact edge will recur for every one of phases 04–11 whose feature component is one of `PaneTree`'s 6 imports or `Meter`'s consumer), or track it as a deferred item.

**Judgment calls:** resume.ts's D16 promotion and the two common→feature edges (mechanical-only, flag-don't-fix) were each preceded by an `advisor()` consult. The D24 harness mechanism's *shape* was sketched and pre-reviewed in the same early consult (route form, `getStaticPaths` gating, `Layout`-not-`Terminal`, per-feature-switch rationale) — the required advisor call immediately before actually settling/building it was skipped in the moment; the settled mechanism (route, spec, project, checklist entries) was reviewed and confirmed sound at the phase-done consult below, with no changes required. Recorded here accurately rather than claiming a consult that didn't happen at that specific point. The `SPLIT_OWNED_RECIPE_NAMES` rename (truthful, since the legacy filter now excludes more than one context's recipes) was mechanical, not a judgment call needing a consult.

**Nothing deferred or left red.** Zero golden churn (42 PNGs before and after, just redistributed). No POM refactor, no behavior change beyond the sanctioned D24 harness addition (new code, not a behavior change to anything existing), no golden regeneration. Phase-done `advisor()` consult confirmed the full phase (all 6 steps, both D20 invocations, golden parity, the D24 settle, the two flagged R004 edges) with no corrections beyond this Results write-up. Ready for verifier/auditor.

# Phase index — v1 → v2 migration

> **Status: phase 03 closed 2026-09-08 (verifier PASS full + 3 scoped rounds; auditor clean after 2 fix rounds + a recommit for per-commit index consistency). Phase 04 (help) in execution per the developer's 2026-09-08 approval. Phases 01–03 closed. ~55 local commits on `frontend-rewrite`, nothing pushed.** Source of truth for rules: `docs/checklist/**`, `docs/testing/**`. Sequencing intent: `Instructions/Planning/test-setup.md`, `Instructions/Planning/delegate.md`. Migration source: the v1 working tree at `/Users/shev/Development/spidey-hub` **as it stands** (uncommitted changes included — the working tree, not HEAD, is what migrates). Each phase's own `PLAN.md` is the executable contract; this file is the only cross-phase document any phase may read.

| # | phase | context | goal | status |
|---|---|---|---|---|
| 01 | pre-phase | out of context | v1 pre-flight, docs alignment, bulk-import green baseline, toolchain (flags/Vitest/oxlint), Playwright `legacy`+`smoke` projects, recipe→feature map, initial design-mirror push | **closed** |
| 02 | common | `src/common` | tmux + vim engines, editor, kernel chrome components, shared lib; kernel+editor specs → `common` project; Terminal.svelte → `bootstrap/` (declared out-of-context wiring) | **closed** |
| 03 | profile | `src/features/profile` | smallest feature — proves the per-feature loop | **closed** |
| 04 | help | `src/features/help` | help views + search; then the delegated fuzzysort swap (D11) | not started |
| 05 | boot | `src/features/boot` | boot sequence + boot state | not started |
| 06 | notifications | `src/features/notifications` | bell/panel/toast system | not started |
| 07 | dashboard | `src/features/dashboard` | dashboard + wordmark + tracker HUD | not started |
| 08 | employment | `src/features/employment` | employment records (editor already in common) | not started |
| 09 | repositories | `src/features/repositories` | largest feature: repo browse, preview, commits, submodule-fed generation | not started |
| 10 | grep | `src/features/grep` | grep overlay; literal-path assertion rewrite | not started |
| 11 | shell-fs | `src/features/shell-fs` | in-window shell; literal-path assertion rewrite | not started |
| 99 | ship | repo history | land closed phases from `frontend-rewrite` → cherry-pick to `main` (runs only when the developer asks; `frontend-rewrite` is an orphan branch with no merge-base with `main`, so conflicts are expected and are reported, never guessed through) | not started |

**Ordering: settled by the developer 2026-08-29 — common runs second** (skill order). Under the bulk-import baseline (D1) the full v1 spec suite gates every phase from day one, which was the protection test-setup.md's kernel-last ordering existed to provide.

## Binding decisions (all phases; do not re-litigate — architecture.md R003)

- **D1 Strategy: bulk-import then restructure in place.** Per-feature e2e/visual needs a bootable app and nothing renders without the Terminal kernel, so v2 cannot be built up from empty. Pre-phase imports v1's whole working tree as a green baseline; later phases move code between contexts with the full suite as a gate. Consequence: `src/components/`+`src/lib/` exist transiently until phase 11 closes; structure audits are phase-scoped until then.
- **D2 toolchain.md R014 settled (user-approved 2026-08-29):** relative TS imports extensionless; `exactOptionalPropertyTypes: true`, `verbatimModuleSyntax: true`. Recorded in toolchain.md during pre-phase.
- **D3 Unit runner is Vitest** (toolchain.md R004). `checklist/testing/README.md` R001 and `docs/testing/e2e/structure.md` still say `node --test` — stale; swept in pre-phase (doc-practice R003/R006). v1's 19 unit tests port mechanically in pre-phase.
- **D5 Visual parity mechanism:** v1's capture pipeline (`pipeline.mjs` + `toMatchSnapshot`) is kept unchanged for the entire migration. Goldens seed from v1 byte-for-byte; `--update-snapshots` is forbidden. Converting to the docs' `toHaveScreenshot` is deferred (see Deferred).
- **D6 Playwright config:** one root `playwright.config.ts`. Pre-phase adds `smoke` (new) + `legacy` (v1's suite as-is). Each phase moves its specs out of `legacy` into its own `<context>` / `<feature>-visual` project entries, added only when the spec folder exists (architecture R002). `legacy` is deleted when it empties (phase 11).
- **D7 Fixture split mapping is fixed here** (per-phase execution): repositories→`fixtures/{repositories,repos,commits}`; employment→`fixtures/personnel`; dashboard→`fixtures/contributions.json`; grep→`fixtures/grep-index.json`; shell-fs→`fixtures/fs-index.json`. Loader/build-script path updates per D22.
- **D8 Literal-path assertions** (grep.spec, shell.spec, any spec asserting real tree paths): updated **in the same change** as the move that invalidates them. `GREP_ROOT_SUBDIRS`/`FS_INDEX_SUBDIRS` stay valid (everything remains under `src/`). No known-red state is ever carried between phases.
- **D9 Editor is common** (repositories + employment previews consume it): `vim.ts` → `common/engines/vim/`, editor components + `editorRender` → `common/components/editor/`, `pasteBuffer.ts` → `common/lib/` (editor + copy-mode). Moves in phase 02.
- **D10 Kernel homes:** `tmux.ts` → `common/engines/tmux/`; chrome (StatusBar, Meter, PaneTree, PanelBadge, Wallpaper, Cmdline, CopyMode, ChooseTree) → `common/components/`; `clock/views/layout/pasteTargets/cmdline/data`.ts → `common/lib/`; Terminal.svelte + terminalState + `layouts/Shell.astro` (renamed `Layout.astro`, D13) → `src/bootstrap/`. **`src/pages/*.astro` and `src/content.config.ts` cannot move — Astro hard-codes both**; pages are thinned to import only from `bootstrap/` (architecture R008); `content.config.ts` aggregates feature-exported collection definitions (R005). Kernel + editor specs → `common/tests/ui/e2e/` under a single `common` project.
- **D11 helpSearch → fuzzysort** (delegate.md): separate, test-verified step inside phase 04, only after the verbatim move is green. Bespoke parts kept: two-shaped corpus, commands-before-keymaps tie-break. Golden-freeze constraint: recipe `20-help-search` renders a fixture query's results — the swap is allowed only if the help visual passes with zero golden churn; otherwise the executor reports back and the swap joins the Deferred list (post-rebaseline). Deliberate `help-search.spec.ts` assertion changes isolated in the swap's own commit.
- **D15 Renames during moves:** plain lib `.ts` → kebab-case at the new home (`bootState.ts` → `boot-state.ts`); state classes keep `<name>State.svelte.ts`; components PascalCase. No `types.ts` or barrel `index.ts` may be carried over (files-and-naming R005/R008 — split by concept during the move). Imports updated in the same change.
- **D16 Rule-of-three:** promote to `common/lib/` only with ≥2 verified real consumers (grep before promoting). Known multi-consumer: `resume.ts` (profile + cmdline), `pasteBuffer.ts`, `docline.ts` (verify at move time). Single-consumer modules go to the owning feature's `lib/`.
- **D17 data.ts / yaml:** `data.ts` → `common/lib/data.ts` in phase 02 with its glob widened to span `src/data/` + `src/common/content/` + `src/features/*/content/`; each feature phase moves its own yaml; the `src/data/` leg is removed when the last yaml leaves (phase 11 closes it out — `site/choosetree/cmdline.yaml` move in phase 02 with the kernel).
- **D18 `exactOptionalPropertyTypes` checkpoint:** if enabling surfaces >~50 type errors in pre-phase, the executor reports the count and stops for orchestrator review.
- **D19 package.json `name` stays `shevinum-dev-v3`** (its text is grep-indexed); rename deferred.
- **D20 Every full-suite gate is TWO invocations:** (a) real build (`E2E_EXPECT_FIXTURES=0`) → smoke + all e2e projects; (b) fixture build (`E2E_EXPECT_FIXTURES=1`, `build:fixtures`) → all `*-visual` + legacy visual. One server, one `dist/`, env-gated; `tests/checkFixtureFlag.ts` fails mismatches by design. Mirror v1's `test:e2e`/`test:visual` split.
- **D21 Visual mechanics settled once, by the FIRST golden-seeding phase (02 or 03, whichever runs first), reused verbatim by every later phase:** (a) v1's `snapshotPathTemplate` assumes `{projectName}` = viewport; per-context project names break that — pick the template + viewport handling that reproduces `.../goldens/<viewport>/<recipe>.png` byte-identically; (b) the shared visual/test infra gets its permanent home in the same step: `pipeline.mjs`, `recipes.ts` (viewports, `CLOCK_TIME`), `static-server.mjs`, `capture-goldens.mjs`, `checkFixtureFlag.ts`, and the vendored `tests/visual/reference/` move to `common/tests/ui/support/` (testing README R002) — before splitting anything, the executor reads `identical.spec.ts`/`adversarial-fixtures.spec.ts` to learn what the port-4400 reference server actually compares against (if "identical" targets the vendored prototype rather than goldens, the split mechanics change — report the finding). Both (a) and (b) are recorded in `Instructions/01-pre-phase/recipe-feature-map.md`; phase 11's remnant-delete list is then empty by construction.
- **D22 `fixtures/` files are build inputs:** `content.config.ts` fixture-switches and `build:fixtures` copy **sources** update in the same change as any fixture move.
- **D24 Feature harness routes (developer-approved 2026-08-29):** every feature phase (03–11) ends with a harness step — a **fixture-build-only** route that mounts just that feature's orchestrator component with seeded fixture props, no Terminal kernel, no tmux chrome. Contract: (a) zero harness pages may exist in a real build (toolchain R010 — the fixture flag never leaks); preferred mechanism is one dynamic route `src/pages/harness/[feature].astro` whose `getStaticPaths` returns `[]` unless `PORTFOLIO_FIXTURES=1` — but the mechanism is settled once by phase 03 and reused verbatim by 04–11 (architecture R003); (b) harness specs live in `src/features/<f>/tests/ui/harness/` under a `<feature>-harness` Playwright project on the fixture build; they assert mount + the feature's core interactions — no goldens for now (harness goldens are a later deliberate choice); (c) phase 03 records the convention as checklist entries (testing/README.md placement rule + a short harness rule where it fits) in the same change, per doc-practice R004; (d) a red harness blocks phase close like any other gate. Purpose: a failing `<feature>-harness` proves that feature itself is broken, independent of the kernel — the isolation testing the bounded-context redesign exists to enable. every phase may make *mechanical* edits outside its context (import paths in the legacy tree/specs, literal-path assertions per D8, one Playwright project entry, `content.config.ts`/`build:fixtures` paths per D22, and — orchestrator ruling 2026-09-08, phase-03 fix round — `data-testid` attribute additions to another context's markup when this phase's tests need a conformant playwright.md R002 locator: attribute-only, zero behavior/pixel change, goldens re-verified in the same change) — declared in its PLAN.md. Behavioral edits outside the context are never in scope.
- **D23 Audit exemptions during migration** (tracked, not permanent — test-setup.md's per-feature exemption): an auditor "no" verdict does **not** block phase close only when it is (a) a POM/locator/spec-convention rule (`e2e-testing.md` R003–R010, `playwright.md` locator rules) hitting a verbatim-ported spec, (b) architecture R009's missing fitness test, or (c) a structure rule hitting the transient legacy tree (`src/components`/`src/lib`) rather than this phase's context. Each exempted "no" is recorded in the phase folder's audit report with the exemption named. **(a) clarified — orchestrator ruling 2026-08-29 (phase-02 audit):** the exemption covers ALL of playwright.md's spec-convention rules for verbatim-ported specs — locator rules (R001–R004) and assertion/waiting rules (R005–R006) alike; rewriting a ported spec's assertion style mid-migration is the same rewrite-the-safety-net hazard test-setup.md forbids. Closes per spec in the deferred POM pass. **(e) — orchestrator ruling 2026-08-29 (phase-02 audit):** pre-existing v1 code-quality patterns relocated *verbatim* into their final home are exempt during migration when fixing them would rewrite moved code (violating the move-don't-rewrite stop conditions): classes.md R003/R004 (field privacy/encapsulation in terminalState/editorState), typescript.md R005 (if-chains over exhaustive switch), tailwind.md R002 (raw `#e0453c` literals), toolchain.md R008 (no formatter configured — repo-wide, predates migration). All close in the post-migration hardening pass (Deferred list). (e) never covers code *authored* during migration. Additionally, **(d) — orchestrator ruling 2026-08-29 (phase-01 re-audit):** toolchain R014's extensionless-import rule is exempted for the *un-ported* legacy spec tree (`tests/e2e/`, `tests/visual/`) only; each feature phase's spec-port step (FX.1) MUST strip `.ts` extensions as part of its mechanical import fixes, so this exemption self-closes feature by feature and is fully closed when `legacy` empties in phase 11. any `.mjs` file run under bare Node (wherever it lives — `scripts/`, `common/tests/ui/support/`, …) importing `.ts` with explicit extensions is not a violation at all — Node's ESM resolver requires the extension (empirically confirmed in the phase-01 re-audit; wording made path-neutral after the phase-02 close audit, since the D21(b) move relocated some of these files). **Orchestrator rulings 2026-09-08 (phase-03 audit):** **(c) extended** — (c) also covers a common→feature import edge whose dependency *direction* pre-existed the move and whose target merely left the transient tree for its real context (`common/components/Meter.svelte`→profile's `net.ts`, `common/components/PaneTree.svelte`→`Profile.svelte`); a path-only update during a move creates no new violation. Closes when the bootstrap-injection registry lands (Deferred hardening item). Edges whose *direction* is authored during migration are never covered. **(e) extended** — files-and-naming structure rules (R002/R010/R012) on a single-file view relocated verbatim (Profile.svelte now; will recur in phases 04–11): splitting a moved view is exactly the rewrite the move-don't-rewrite stop conditions forbid; closes in the post-migration hardening pass. **architecture R006 vs D16** — `resume.ts` stays in `common/lib/` per D16 (binding; R003 forbids re-litigating); its `window.open` DOM call is a recorded impurity, and whether it re-homes (e.g. a DOM-allowed common area) is a hardening-pass decision. **architecture R008 vs D24** — the harness route's direct feature-component mount is D24's sanctioned design (kernel-free isolation is the harness's purpose), not an R008 violation; harness routes are exempt from R008 for the life of D24's mechanism. **Orchestrator rulings 2026-09-08 (phase-04 audit):** **(b) extended** — D23(b) covers testing/README.md R008/R009 alongside architecture.md R009: all three describe the same missing fitness/audit mechanism, which lands as one deliverable in the deferred boundary-fitness follow-up pass. **Shared kernel-chrome page objects (e2e-testing R005)** — a feature page object may not redefine kernel-chrome locators (`status-bar-windows` etc.); the shared class lives in `common/tests/ui/pages/` and is composed. The first phase whose audit catches the duplication fixes ALL standing copies (including ones in closed phases — a test-infra-only, behavior-free edit, sanctioned as a declared mechanical out-of-context fix) so the duplication never compounds. Every other "no" blocks.
- **Commit discipline:** single-line imperative, **no body, no trailer** (toolchain R011 — orchestrator ruling 2026-08-29: the checklist wins over the harness's default Co-Authored-By trailer; an earlier version of this line mandated the trailer while citing R011, a self-contradiction the phase-01 audit caught), one reviewable unit per commit (R012), committed to **`frontend-rewrite`** at phase close. Commits stay local; pushing to `origin/frontend-rewrite` only when the developer says so.

## Open questions (developer)

1. ~~Git setup~~ **Resolved 2026-08-29:** this directory is the checkout of `ShevinuM/spidey-hub`'s `frontend-rewrite` branch (orphan; its first commit holds the planning docs), tracking origin. Phase closes commit to `frontend-rewrite`; ship cherry-picks onto `main` in the same repo. The four locally-modified docs (`delegate.md`, checklist README, `design-mirror.md`, `toolchain.md`) predate this plan and get committed with pre-phase's docs-alignment step.
2. **Phase ordering** — see the ordering note above (common second vs kernel-last).

## Deferred (doc-practice R008)

- **D11 fuzzysort swap (reverted out of phase 04, 2026-09-08):** fuzzysort's continuous scorer structurally reorders the golden query's tied results (`kill-pane` above `kill-window`, `0.8836` vs `0.8656` on bare labels — not tunable), so the swap failed `20-help-search`'s zero-churn gate at both viewports and was fully reverted per D11's own contract. Lands only with a deliberate rebaseline of `20-help-search`'s goldens (both viewports) in the post-migration rebaseline pass — not via a cleverer config.
- **Harness dead-chunk cleanup (orchestrator ruling 2026-09-08, phase-04 close):** a harness-only wrapper island leaves an unreferenced `_astro/<X>Harness.<hash>.js` chunk (~1 KB) in the real build even though `dist/harness/` route output is absent. Ruled harmless for the migration — the D24 contract ("zero harness pages in a real build; the fixture flag never leaks") holds: no page, no route, no fixture data, just a dead unreferenced chunk inherent to how Astro bundles islands. An env-gated-island cleanup joins the post-migration hardening pass; phases 05–11 may ship the same dead chunk without a new ruling.
- `toHaveScreenshot` convergence + full R005 rebaseline (post-migration, deliberate change).
- POM/locator refactor of ported specs (test-setup.md step 5) — tracked per-feature exemption; closed per feature in a follow-up pass after phase 11, not inside migration phases.
- Boundary fitness test in `tests/audits/` (architecture R009) + testid prefix audit (files-and-naming R014) — follow-up pass after phase 11.
- CI workflow; Playwright version bump; package rename; README rewrite; `docs/screenshots/` refresh.
- **Post-migration hardening pass** (D23(e) closure): classes.md R003/R004 in `terminalState`/`editorState` (`#` fields, encapsulate public `$state`), typescript.md R005 exhaustive switches, tailwind.md R002 `var(--color-alert)` sweep (28 sites), formatter adoption (toolchain R008), and `views.ts`'s hardcoded feature registry (common/lib knowing the full feature list — registry should be injected from bootstrap; flagged by the phase-02 audit, no current rule reaches it).

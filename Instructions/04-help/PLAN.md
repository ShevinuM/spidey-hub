# Phase 04 — help

> **Status: steps 1–5 (the per-feature loop) complete and green; steps 6–7 in progress.** Binding decisions: `Instructions/00-phases.md` — especially **D11** (fuzzysort) and its golden-freeze constraint.

## Context

`src/features/help`. Out-of-context wiring: project entries, legacy fallout, `content.config.ts` pointer for `content/help`, D22.

## Objective

Help views + `?` fuzzy search migrated verbatim and green; then, as a separate verified step, the hand-rolled matcher delegated to `fuzzysort` (delegate.md's one real action item) — bespoke corpus + tie-break kept.

## Background — move table

| v1 | v2 |
|---|---|
| `src/components/HelpView.svelte`, `src/components/HelpSearch.svelte` | `src/features/help/components/` |
| `src/lib/helpSearch.ts` | `src/features/help/lib/help-search.ts` |
| `src/lib/docline.ts` | per D16: `common/lib/docline.ts` if ≥2 feature consumers verified by grep, else help's `lib/` |
| `src/content/help/` (13 docs), `src/data/{help,helpsearch}.yaml` | `src/features/help/content/` (+ `content.config.ts`, `data.ts` glob per D17) |
| specs `help, help-layout, help-search` `.spec.ts`; unit `helpSearch, docline` `.test.ts` | `tests/ui/e2e/`; `tests/unit/` |
| help-mapped goldens incl. `20-help-search` | `tests/ui/visual/goldens/<viewport>/` |

## Steps

- [x] **1–5.** The per-feature loop exactly as `Instructions/03-profile/PLAN.md` defines it (verbatim spec port → source move → full D20 gate → golden parity → unit-test move), with this phase's table. D21 mechanics reused verbatim.
  *Verify (D20):* real build → `--project=help` + `--project=legacy` green against un-moved source; fixture build → `--project=help-visual` green; `cmp` clean.
  *Result:* done, split into 3 commits matching the loop's natural seams (port, move, unit-test move), with a full re-verification (step 3) run from the clean committed tree in between.

  **Step 1 (commit `99d2a54`, "Port help specs and goldens to src/features/help"):** `tests/e2e/{help,help-layout,help-search}.spec.ts` moved (`git mv`, not copied) to `src/features/help/tests/ui/e2e/`. Imports fixed: `fixtures`/`content-fixtures` re-pointed to `../../../../../../common/tests/ui/support/{fixtures,content-fixtures}` (extensionless, D23(d)/FX.1 — 6 levels up, verified with `node -e "path.relative(...)"`); each spec's `ROOT = join(import.meta.dirname, ...)` depth fixed from `"../.."` to `"../../../../../.."` (6 levels, same reasoning). `help.spec.ts`'s sibling-file comment (pointing at `help-search.spec.ts`) fixed to the new same-folder reference. Two recipes owned by this phase, in two different `recipes.ts` arrays (`"11-help"` in `extraRecipes`, `"20-help-search"` in `iteration3Recipes` — profile's step 1 only ever needed to filter one array, so this doesn't copy verbatim): new `src/features/help/tests/ui/visual/identical.spec.ts` imports and filters both to `HELP_OWNED_RECIPE_NAMES`. Goldens seeded via `cp` to `src/features/help/tests/ui/visual/goldens/{1512x945,1920x1080}/{11-help,20-help-search}.png`, `cmp`-verified byte-identical against both the pre-move `tests/visual/goldens/` copies and v1's own copies at `/Users/shev/Development/spidey-hub`, then the now-orphaned originals removed. `tests/visual/identical.spec.ts`'s `SPLIT_OWNED_RECIPE_NAMES` gained `"11-help"`/`"20-help-search"`, header comment updated to record phase 04's ownership. Added `help-<viewport>`/`help-visual-<viewport>` project entries to `playwright.config.ts` (own `snapshotPathTemplate`, viewport hardcoded — D21(a) reused verbatim, not `{projectName}`). `package.json`: `test:e2e` gained `--project=help-1512x945 --project=help-1920x1080`; `test:visual` gained the new visual spec's path. Both `docs/testing/{e2e,visual}/running-tests.md` swept in this same commit (doc-practice R003): `help` removed from the still-legacy lists, new rows added for `help-*`/`help-visual-*`, each verified with `playwright test --list` before writing.
  Verified against **un-moved source**: real build (`pnpm build`) succeeded; D20 real-build invocation (`smoke`+`legacy×2`+`common×2`+`profile×2`+`help×2`) → **1012/1012 passed**. Fixture build (`pnpm build:fixtures`) succeeded; D20 fixture-build invocation (all `*-visual`/`identical.spec.ts` files + `profile-harness`) → **53/53 passed**. `public/generated/{grep-index,fs-index}.json` diffed programmatically (not just `git diff`, since both are single-line JSON) — only the 3 spec-file path renames plus the 1 new visual-spec path, nothing else. Unrelated live-data drift (`contributions.json`/`commits/daily-tech-digest.json`/`file-icons.json`) reverted before committing.

  **Step 2 (commit `9a908ec`, "Move HelpView/HelpSearch/help-search/docline and help content to their v2 homes"):** via `git mv`: `HelpView.svelte`/`HelpSearch.svelte` → `src/features/help/components/`; `helpSearch.ts` → `src/features/help/lib/help-search.ts` (D15 kebab-case rename); the 13 `content/help/*.md` docs + `help.yaml`/`helpsearch.yaml` → `src/features/help/content/` (D17 — yaml joins its feature's own content dir, same shape D17 already used in phase 02/03). **`docline.ts` → `src/common/lib/docline.ts`** (D16): grepped real consumers first (not comment mentions) — exactly 3, none of them help itself: `src/components/Shell.svelte` (shell-fs), `src/components/repositories/repositoriesState.svelte.ts` (repositories), `src/components/employment-records/employmentRecordsState.svelte.ts` (employment). ≥2 real consumers in different (future) feature contexts, confirmed by `grep -n "docline" src/components/HelpView.svelte src/components/HelpSearch.svelte` returning nothing → docline has zero coupling to help and is a stronger rule-of-three case than profile's `resume.ts` (3 consumers vs. 2, and none in the owning feature) → promoted to `common/lib/`.
  Imports fixed: `HelpView.svelte`/`HelpSearch.svelte` (`../common/lib/data`, `../common/components/PanelBadge.svelte`, `../lib/helpSearch`, `../common/lib/{paste-targets,layout}` → `../../../common/lib/...` / `../lib/help-search`, verified via `path.relative`); `Shell.svelte`/`repositoriesState.svelte.ts`/`employmentRecordsState.svelte.ts` (`../lib/docline` / `../../lib/docline` → `../common/lib/docline` / `../../common/lib/docline`); `src/bootstrap/Terminal.svelte`'s `HelpSearch` import → `../features/help/components/HelpSearch.svelte` (bootstrap→feature, sanctioned); `content.config.ts`'s `help` collection `base` → `src/features/help/content`; `src/common/lib/data.ts`'s `helpRaw`/`helpsearchRaw` `?raw` imports re-pointed to `../../features/help/content/{help,helpsearch}.yaml` (D17's "widened glob" — confirmed, same as phase 02's finding, this file has no `import.meta.glob`, just 13 explicit per-file `?raw` imports; "widened" means each moved file's own import is re-pointed), plus its header comment and the `help.yaml`/`src/content/help` section-banner comment updated for accuracy.
  **One common→feature edge, mechanical-only per this phase's scope, flagged for the auditor (same channel/pattern as phase 03's Meter/PaneTree findings):** `src/common/components/PaneTree.svelte` (already flagged by phases 02 and 03 for importing then-transient/now-migrated feature components) now imports `../../features/help/components/HelpView.svelte` directly — the same D23(c)-extended R004 violation recurring exactly as phase 03 predicted it would. Only the import path was updated; the dependency direction pre-existed the move.
  Comment sweep (comments.md R003, same-change): `PreviewPanel.svelte`'s `src/lib/docline.ts` path reference fixed; `tests/unit/docline.test.ts`/`tests/unit/helpSearch.test.ts` imports fixed in place (moved to their final feature/common homes in step 5, per the plan's step ordering — same reasoning phase 03 used for `net.test.ts`).
  `pnpm generate` re-run after the move; `public/generated/{grep-index,fs-index}.json` diffed programmatically — only the moved paths changed (2 components, 1 lib module, 13 content docs, 2 yaml, 1 docline.ts), nothing else. Unrelated live-data drift reverted before committing.
  Old-path verify: `test -d src/content/help` / `test -f src/data/{help,helpsearch}.yaml` / `test -f src/lib/{docline,helpSearch}.ts` / `test -f src/components/Help{View,Search}.svelte` all report removed; `grep -rn "src/lib/docline\|src/lib/helpSearch\|src/components/HelpView\|src/components/HelpSearch\|src/data/help\.yaml\|src/data/helpsearch\.yaml\|src/content/help/"` across `src`/`tests`/`common`/`scripts`/`*.astro`/`*.mjs` shows no stale residue (one historical mention in v1-imported `tests/visual/README-PIPELINE.md` describing a past capture event at the path that was true at that time — left untouched, out of scope).

  **Step 3 (full gate, no separate commit — folded into this Results recording):** re-run from the clean, fully-committed tree (`git status` empty apart from the pre-existing untracked `.claude/`) after step 2's commit. `pnpm check`: 0 errors, 952 files, 10 pre-existing warnings (unrelated, not touched this phase), 4 files-with-problems (pre-existing). `oxlint`: exit 0, no findings. `pnpm test:unit`: 339/339 passed, 19/19 files. D20 real-build invocation (`smoke`+`legacy×2`+`common×2`+`profile×2`+`help×2`): **1012/1012 passed** (2.6m — this run auto-backgrounded past the 120s foreground default; monitored to completion via the harness's Monitor tool rather than sleep-polling). D20 fixture-build invocation (all `*-visual`/`identical.spec.ts` files + `profile-harness`): **53/53 passed**. `public/generated/{grep-index,fs-index}.json` unchanged by this re-run (already regenerated/committed in step 2); unrelated live-data drift from re-running the builds reverted before reporting.

  **Step 4 (golden parity, no separate commit):** `diff -rq tests/visual/goldens <v1>/tests/visual/goldens` reports exactly 7 "Only in v1" files per viewport (`06-editor`/`15-cmdline`/`18-split`/`19-choose-tree` from phase 02, `07-profile` from phase 03, `11-help`/`20-help-search` from this phase) — zero `differ` lines. `cmp`-verified byte-identical: both help goldens at both viewports, against both the pre-move tree and v1. Total PNG count unchanged: 42 (21 recipes × 2 viewports), now redistributed across `tests/visual/goldens/` (14 remaining) + `common/tests/ui/visual/goldens/` (4) + `src/features/profile/tests/ui/visual/goldens/` (1) + `src/features/help/tests/ui/visual/goldens/` (2, this phase).

  **Step 5 (commit `acb3156`, "Move help-search and docline unit tests to their v2 homes"):** `git mv tests/unit/helpSearch.test.ts src/features/help/tests/unit/help-search.test.ts` (D15 kebab-case rename, matching its module) — import fixed to `../../lib/help-search` (one level shallower now that the test lives inside the feature's own tree). **`docline.test.ts` + `docline.fixtures.json` → `common/tests/unit/`, not help's `tests/unit/`** — a deliberate deviation from the move table's literal wording, since the table itself conditions docline's home on D16's verdict and D16 sent the module to `common/lib/`; test-follows-module is the same convention phase 02/03 established. Import fixed to `../../../src/common/lib/docline`; `ROOT` depth fixed from `"../.."` to `"../../.."` (`common/tests/unit` is 3 levels deep, not 2, verified with `path.relative`); the sibling `fixturesPath` join (`import.meta.dirname` + `"docline.fixtures.json"`) needed no change, since the fixture file moved as its sibling. No `vitest.config.ts` change needed — its glob already covers both `common/tests/unit/**/*.test.ts` and `src/features/*/tests/unit/**/*.test.ts` (phase 02 forward-declared both). Comment sweep in the same commit: `docline.ts`'s header comment path reference and `help-search.spec.ts`'s two remaining `tests/unit/helpSearch.test.ts` mentions (one header, one inline) fixed to their new locations. `pnpm test:unit`: 339/339 passed, 19/19 files — identical count to before the move (pure relocation); confirmed `common/`'s own tests/ tree is NOT walked by `scripts/generate.mjs`'s grep/fs indexes (`GREP_ROOT_SUBDIRS = ["src", "scripts", "tests"]`, no `common`), so `docline.test.ts`/`docline.fixtures.json` correctly drop out of both indexes entirely on this move (not a bug) while `help-search.test.ts` correctly appears under its new `src/...` path — verified programmatically before committing.

- [ ] **6. Fuzzysort swap (D11) — only after 1–5 are verified green.** Add exact-pinned `fuzzysort`; replace the tiered cascade + hand-written Levenshtein in `help-search.ts`; keep the two-shaped corpus building (`cmdline.yaml`/`content/help`/`shell.yaml`) and the commands-before-keymap-rows tie-break as a thin sort after scoring; delete dead code.
  *Verify:* unit + `--project=help` green (real build); **fixture build → `--project=help-visual` green with zero golden churn** — recipe `20-help-search`'s pixels are the hard gate. If ranking for the golden query changes: revert the swap, report back, the swap moves to Deferred (post-rebaseline). Deliberate `help-search.spec.ts` assertion changes allowed only if pixels hold, isolated in this step's own commit.

- [ ] **7. Feature harness (D24).** Reuse phase 03's settled mechanism: harness route mounting the help orchestrator (both `HelpView` and the `?` search palette reachable) with seeded fixture props; specs in `tests/ui/harness/`, project `help-harness` (fixture build).
  *Verify:* fixture build → `--project=help-harness` green; real build → no `harness/` output in `dist/`.

## Acceptance criteria

Loop + swap + harness verified (or swap explicitly reverted-and-reported); verifier PASS + auditor clean on `src/features/help` (D23); commit discipline per 00-phases.md. Stop: no search-UX changes beyond what fuzzysort's scoring implies within the golden constraint.

## Results

(executor fills in)

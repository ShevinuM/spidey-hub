# Phase 04 — help

> **Status: all 7 steps complete.** Steps 1–5 (loop) and 7 (harness) verified green; step 6 (fuzzysort swap) closed via the plan's own sanctioned revert-and-report path — the D11 golden-freeze gate did not hold, so the swap moved to Deferred rather than landing. Binding decisions: `Instructions/00-phases.md` — especially **D11** (fuzzysort) and its golden-freeze constraint.

## Context

`src/features/help`. Out-of-context wiring: project entries, legacy fallout, `content.config.ts` pointer for `content/help`, D22.

## Objective

Help views + `?` fuzzy search migrated verbatim and green; then, as a separate verified step, the hand-rolled matcher delegated to `fuzzysort` (delegate.md's one real action item) — bespoke corpus + tie-break kept.

## Background — move table

| v1 | v2 |
|---|---|
| `src/components/HelpView.svelte`, `src/components/HelpSearch.svelte` | `src/features/help/components/` |
| `src/lib/helpSearch.ts` | `src/features/help/lib/help-search.ts` |
| `src/lib/docline.ts` | per D16: `common/lib/docline.ts` if ≥2 feature consumers verified by grep, else help's `lib/` |
| `src/content/help/` (13 docs), `src/data/{help,helpsearch}.yaml` | `src/features/help/content/` (+ `content.config.ts`, `data.ts` glob per D17) |
| specs `help, help-layout, help-search` `.spec.ts`; unit `helpSearch, docline` `.test.ts` | `tests/ui/e2e/`; `tests/unit/` |
| help-mapped goldens incl. `20-help-search` | `tests/ui/visual/goldens/<viewport>/` |

## Steps

- [x] **1–5.** The per-feature loop exactly as `Instructions/03-profile/PLAN.md` defines it (verbatim spec port → source move → full D20 gate → golden parity → unit-test move), with this phase's table. D21 mechanics reused verbatim.
  *Verify (D20):* real build → `--project=help` + `--project=legacy` green against un-moved source; fixture build → `--project=help-visual` green; `cmp` clean.
  *Result:* done, split into 3 commits matching the loop's natural seams (port, move, unit-test move), with a full re-verification (step 3) run from the clean committed tree in between.

  **Step 1 (commit `99d2a54`, "Port help specs and goldens to src/features/help"):** `tests/e2e/{help,help-layout,help-search}.spec.ts` moved (`git mv`, not copied) to `src/features/help/tests/ui/e2e/`. Imports fixed: `fixtures`/`content-fixtures` re-pointed to `../../../../../../common/tests/ui/support/{fixtures,content-fixtures}` (extensionless, D23(d)/FX.1 — 6 levels up, verified with `node -e "path.relative(...)"`); each spec's `ROOT = join(import.meta.dirname, ...)` depth fixed from `"../.."` to `"../../../../../.."` (6 levels, same reasoning). `help.spec.ts`'s sibling-file comment (pointing at `help-search.spec.ts`) fixed to the new same-folder reference. Two recipes owned by this phase, in two different `recipes.ts` arrays (`"11-help"` in `extraRecipes`, `"20-help-search"` in `iteration3Recipes` — profile's step 1 only ever needed to filter one array, so this doesn't copy verbatim): new `src/features/help/tests/ui/visual/identical.spec.ts` imports and filters both to `HELP_OWNED_RECIPE_NAMES`. Goldens seeded via `cp` to `src/features/help/tests/ui/visual/goldens/{1512x945,1920x1080}/{11-help,20-help-search}.png`, `cmp`-verified byte-identical against both the pre-move `tests/visual/goldens/` copies and v1's own copies at `/Users/shev/Development/spidey-hub`, then the now-orphaned originals removed. `tests/visual/identical.spec.ts`'s `SPLIT_OWNED_RECIPE_NAMES` gained `"11-help"`/`"20-help-search"`, header comment updated to record phase 04's ownership. Added `help-<viewport>`/`help-visual-<viewport>` project entries to `playwright.config.ts` (own `snapshotPathTemplate`, viewport hardcoded — D21(a) reused verbatim, not `{projectName}`). `package.json`: `test:e2e` gained `--project=help-1512x945 --project=help-1920x1080`; `test:visual` gained the new visual spec's path. Both `docs/testing/{e2e,visual}/running-tests.md` swept in this same commit (doc-practice R003): `help` removed from the still-legacy lists, new rows added for `help-*`/`help-visual-*`, each verified with `playwright test --list` before writing.
  Verified against **un-moved source**: real build (`pnpm build`) succeeded; D20 real-build invocation (`smoke`+`legacy×2`+`common×2`+`profile×2`+`help×2`) → **1012/1012 passed**. Fixture build (`pnpm build:fixtures`) succeeded; D20 fixture-build invocation (all `*-visual`/`identical.spec.ts` files + `profile-harness`) → **53/53 passed**. `public/generated/{grep-index,fs-index}.json` diffed programmatically (not just `git diff`, since both are single-line JSON) — only the 3 spec-file path renames plus the 1 new visual-spec path, nothing else. Unrelated live-data drift (`contributions.json`/`commits/daily-tech-digest.json`/`file-icons.json`) reverted before committing.

  **Step 2 (commit `9a908ec`, "Move HelpView/HelpSearch/help-search/docline and help content to their v2 homes"):** via `git mv`: `HelpView.svelte`/`HelpSearch.svelte` → `src/features/help/components/`; `helpSearch.ts` → `src/features/help/lib/help-search.ts` (D15 kebab-case rename); the 13 `content/help/*.md` docs + `help.yaml`/`helpsearch.yaml` → `src/features/help/content/` (D17 — yaml joins its feature's own content dir, same shape D17 already used in phase 02/03). **`docline.ts` → `src/common/lib/docline.ts`** (D16): grepped real consumers first (not comment mentions) — exactly 3, none of them help itself: `src/components/Shell.svelte` (shell-fs), `src/components/repositories/repositoriesState.svelte.ts` (repositories), `src/components/employment-records/employmentRecordsState.svelte.ts` (employment). ≥2 real consumers in different (future) feature contexts, confirmed by `grep -n "docline" src/components/HelpView.svelte src/components/HelpSearch.svelte` returning nothing → docline has zero coupling to help and is a stronger rule-of-three case than profile's `resume.ts` (3 consumers vs. 2, and none in the owning feature) → promoted to `common/lib/`.
  Imports fixed: `HelpView.svelte`/`HelpSearch.svelte` (`../common/lib/data`, `../common/components/PanelBadge.svelte`, `../lib/helpSearch`, `../common/lib/{paste-targets,layout}` → `../../../common/lib/...` / `../lib/help-search`, verified via `path.relative`); `Shell.svelte`/`repositoriesState.svelte.ts`/`employmentRecordsState.svelte.ts` (`../lib/docline` / `../../lib/docline` → `../common/lib/docline` / `../../common/lib/docline`); `src/bootstrap/Terminal.svelte`'s `HelpSearch` import → `../features/help/components/HelpSearch.svelte` (bootstrap→feature, sanctioned); `content.config.ts`'s `help` collection `base` → `src/features/help/content`; `src/common/lib/data.ts`'s `helpRaw`/`helpsearchRaw` `?raw` imports re-pointed to `../../features/help/content/{help,helpsearch}.yaml` (D17's "widened glob" — confirmed, same as phase 02's finding, this file has no `import.meta.glob`, just 13 explicit per-file `?raw` imports; "widened" means each moved file's own import is re-pointed), plus its header comment and the `help.yaml`/`src/content/help` section-banner comment updated for accuracy.
  **One common→feature edge, mechanical-only per this phase's scope, flagged for the auditor (same channel/pattern as phase 03's Meter/PaneTree findings):** `src/common/components/PaneTree.svelte` (already flagged by phases 02 and 03 for importing then-transient/now-migrated feature components) now imports `../../features/help/components/HelpView.svelte` directly — the same D23(c)-extended R004 violation recurring exactly as phase 03 predicted it would. Only the import path was updated; the dependency direction pre-existed the move.
  Comment sweep (comments.md R003, same-change): `PreviewPanel.svelte`'s `src/lib/docline.ts` path reference fixed; `tests/unit/docline.test.ts`/`tests/unit/helpSearch.test.ts` imports fixed in place (moved to their final feature/common homes in step 5, per the plan's step ordering — same reasoning phase 03 used for `net.test.ts`).
  `pnpm generate` re-run after the move; `public/generated/{grep-index,fs-index}.json` diffed programmatically — only the moved paths changed (2 components, 1 lib module, 13 content docs, 2 yaml, 1 docline.ts), nothing else. Unrelated live-data drift reverted before committing.
  Old-path verify: `test -d src/content/help` / `test -f src/data/{help,helpsearch}.yaml` / `test -f src/lib/{docline,helpSearch}.ts` / `test -f src/components/Help{View,Search}.svelte` all report removed; `grep -rn "src/lib/docline\|src/lib/helpSearch\|src/components/HelpView\|src/components/HelpSearch\|src/data/help\.yaml\|src/data/helpsearch\.yaml\|src/content/help/"` across `src`/`tests`/`common`/`scripts`/`*.astro`/`*.mjs` shows no stale residue (one historical mention in v1-imported `tests/visual/README-PIPELINE.md` describing a past capture event at the path that was true at that time — left untouched, out of scope).

  **Step 3 (full gate, no separate commit — folded into this Results recording):** re-run from the clean, fully-committed tree (`git status` empty apart from the pre-existing untracked `.claude/`) after step 2's commit. `pnpm check`: 0 errors, 952 files, 10 pre-existing warnings (unrelated, not touched this phase), 4 files-with-problems (pre-existing). `oxlint`: exit 0, no findings. `pnpm test:unit`: 339/339 passed, 19/19 files. D20 real-build invocation (`smoke`+`legacy×2`+`common×2`+`profile×2`+`help×2`): **1012/1012 passed** (2.6m — this run auto-backgrounded past the 120s foreground default; monitored to completion via the harness's Monitor tool rather than sleep-polling). D20 fixture-build invocation (all `*-visual`/`identical.spec.ts` files + `profile-harness`): **53/53 passed**. `public/generated/{grep-index,fs-index}.json` unchanged by this re-run (already regenerated/committed in step 2); unrelated live-data drift from re-running the builds reverted before reporting.

  **Step 4 (golden parity, no separate commit):** `diff -rq tests/visual/goldens <v1>/tests/visual/goldens` reports exactly 7 "Only in v1" files per viewport (`06-editor`/`15-cmdline`/`18-split`/`19-choose-tree` from phase 02, `07-profile` from phase 03, `11-help`/`20-help-search` from this phase) — zero `differ` lines. `cmp`-verified byte-identical: both help goldens at both viewports, against both the pre-move tree and v1. Total PNG count unchanged: 42 (21 recipes × 2 viewports), now redistributed across `tests/visual/goldens/` (14 remaining) + `common/tests/ui/visual/goldens/` (4) + `src/features/profile/tests/ui/visual/goldens/` (1) + `src/features/help/tests/ui/visual/goldens/` (2, this phase).

  **Step 5 (commit `acb3156`, "Move help-search and docline unit tests to their v2 homes"):** `git mv tests/unit/helpSearch.test.ts src/features/help/tests/unit/help-search.test.ts` (D15 kebab-case rename, matching its module) — import fixed to `../../lib/help-search` (one level shallower now that the test lives inside the feature's own tree). **`docline.test.ts` + `docline.fixtures.json` → `common/tests/unit/`, not help's `tests/unit/`** — a deliberate deviation from the move table's literal wording, since the table itself conditions docline's home on D16's verdict and D16 sent the module to `common/lib/`; test-follows-module is the same convention phase 02/03 established. Import fixed to `../../../src/common/lib/docline`; `ROOT` depth fixed from `"../.."` to `"../../.."` (`common/tests/unit` is 3 levels deep, not 2, verified with `path.relative`); the sibling `fixturesPath` join (`import.meta.dirname` + `"docline.fixtures.json"`) needed no change, since the fixture file moved as its sibling. No `vitest.config.ts` change needed — its glob already covers both `common/tests/unit/**/*.test.ts` and `src/features/*/tests/unit/**/*.test.ts` (phase 02 forward-declared both). Comment sweep in the same commit: `docline.ts`'s header comment path reference and `help-search.spec.ts`'s two remaining `tests/unit/helpSearch.test.ts` mentions (one header, one inline) fixed to their new locations. `pnpm test:unit`: 339/339 passed, 19/19 files — identical count to before the move (pure relocation); confirmed `common/`'s own tests/ tree is NOT walked by `scripts/generate.mjs`'s grep/fs indexes (`GREP_ROOT_SUBDIRS = ["src", "scripts", "tests"]`, no `common`), so `docline.test.ts`/`docline.fixtures.json` correctly drop out of both indexes entirely on this move (not a bug) while `help-search.test.ts` correctly appears under its new `src/...` path — verified programmatically before committing.

- [x] **6. Fuzzysort swap (D11) — only after 1–5 are verified green.** Add exact-pinned `fuzzysort`; replace the tiered cascade + hand-written Levenshtein in `help-search.ts`; keep the two-shaped corpus building (`cmdline.yaml`/`content/help`/`shell.yaml`) and the commands-before-keymap-rows tie-break as a thin sort after scoring; delete dead code.
  *Verify:* unit + `--project=help` green (real build); **fixture build → `--project=help-visual` green with zero golden churn** — recipe `20-help-search`'s pixels are the hard gate. If ranking for the golden query changes: revert the swap, report back, the swap moves to Deferred (post-rebaseline). Deliberate `help-search.spec.ts` assertion changes allowed only if pixels hold, isolated in this step's own commit.
  *Result:* **reverted and reported — the golden-freeze gate did not hold.** No commit landed for this step; `git status` is clean of any fuzzysort trace (package.json/pnpm-lock.yaml/help-search.ts all back to their step-5 state).

  **What was tried:** `pnpm add -E fuzzysort@4.0.2` (dependencies, no caret, matching the repo's exact-pin convention); read the installed package's own `index.d.ts` (not written from memory — v4's `go(search, targets, options)` returns score-descending `KeysResults<T>` when given a `keys` array of getter functions, `score` is continuous, 1 = exact ... 0 = no match, default `threshold: 0.5`). Before touching the fixture gate, an offline scratchpad script (run against the repo, not committed) loaded the REAL corpus — `src/common/content/cmdline.yaml`, `src/features/help/content/*.md` frontmatter rows, `src/data/shell.yaml`'s `help.rows` — none of which are fixture-switched, so this corpus is identical to what recipe `20-help-search` actually renders. It ran the old hand-rolled `searchHelp()` and a candidate fuzzysort-based replacement (`keys: [label, description, aliasText]`, `threshold: 0`, own `entries` array index as the corpus-order tie-break on exact score ties, per delegate.md) side by side against the canaries: `"kil"`, `"dash"`, `"rbt"`, `"neof"`, `"reboot"`.
  Result: only `"reboot"` (a full-word query) produced an identical top-10 order. **`"kil"` — the golden recipe's own query — did not**: old ranks `kill-window` before `kill-pane` (both "prefix, distance 0" in the old tiered scheme, tied, resolved by corpus order — kill-window's help-content row comes first); fuzzysort's continuous scorer ranks `kill-pane` above `kill-window` (`0.8836` vs `0.8656`, confirmed even searching the two bare labels alone with no other fields involved) because it's sensitive to target *length*, not just "does the query occur verbatim". This is a structural mismatch, not a tuning gap: delegate.md's tie-break design assumes fuzzysort returns *equal* scores for cases the old cascade flattened into one tier, and it doesn't — a continuous scorer essentially never ties two different strings exactly. The only way to force the old grouping back would be a `scoreFn` that buckets/quantizes scores to reproduce the tiers, which is the hand-rolled cascade reimplemented *inside* the library call — exactly what D11 exists to delete, not a legitimate configuration of the delegated library.
  **Real-gate confirmation (dress rehearsal, working tree only, never committed):** swapped `searchHelp()`'s body in `src/features/help/lib/help-search.ts` for the fuzzysort version above (import aliased `fuzzysortDressRehearsal`, `bestMatch`/cascade helpers left in place unused so it still type-checked — `pnpm check`: 0 errors). `pnpm build:fixtures` + `playwright test src/features/help/tests/ui/visual/identical.spec.ts`: **`11-help` passed at both viewports** (it never renders the palette, unaffected); **`20-help-search` FAILED at both viewports** — 7012 px diff (1512×945), 7000 px diff (1920×1080). Read the diff image (playwright.md R011, not just the pixel count): the entire diff is confined to the results-list rows — `kill-pane`/`kill-window` (and their neighbors) reordered, exactly the mismatch the offline check predicted, nothing else on the page shifted.
  **Full revert:** `git checkout -- src/features/help/lib/help-search.ts package.json pnpm-lock.yaml`; `node_modules/fuzzysort` removed and `pnpm install` re-run to drop it from the lockfile/store. `git status`: clean (only the pre-existing untracked `.claude/`). Re-ran `pnpm build:fixtures` + the same spec: **4/4 passed** — proof the revert is complete, not just visually plausible.
  As a side note (not a reason to reconsider — pixels are the named hard gate, not e2e behavior): `help-search.spec.ts`'s own e2e canaries would likely still have passed under the swap (`"kil"` still surfaces both `kill-window` and `kill-pane` somewhere in the list, `"dash"`/`"reboot"`/`"neof"` all still rank their expected top result first) — only the pixel gate catches the reordering, which is exactly why D11 named pixels as the hard gate rather than the existing e2e suite.
  **Consequence:** the swap moves to Deferred, needing a **deliberate rebaseline of `20-help-search`'s goldens** (both viewports) in the same future change that lands it — not a cleverer fuzzysort configuration. `Instructions/00-phases.md`'s Deferred list is the orchestrator's document to edit (every prior entry there landed via an orchestrator/phase-index commit, not an executor one); recommending the addition in this phase's final report rather than editing it directly.

- [x] **7. Feature harness (D24).** Reuse phase 03's settled mechanism: harness route mounting the help orchestrator (both `HelpView` and the `?` search palette reachable) with seeded fixture props; specs in `tests/ui/harness/`, project `help-harness` (fixture build).
  *Verify:* fixture build → `--project=help-harness` green; real build → no `harness/` output in `dist/`.
  *Result:* done (commit `be78b6c`, "Add help feature harness route, wrapper, page object, and spec"). Unlike profile, help has no single top-level component for the harness to mount — `HelpView.svelte`/`HelpSearch.svelte` are two independently-mounted islands Terminal.svelte composes at runtime — so this step needed one genuinely new piece beyond profile's mechanism: a harness-only wrapper, `src/features/help/tests/ui/harness/HelpHarness.svelte`. It mounts both components and implements the minimal 3-way keydown split needed to reach both (palette open → forward to it; bare `?` → open it; else → forward to `HelpView`) — no gating logic, since nothing here can ever be open to gate against (no editor/grep/Cmdline exist in isolation). Lives beside its own harness spec, not in the feature's real `components/` tree, since it's test support, not production UI. `svelte-autofixer` run against it (0 issues after switching a mount flag from `$state`+`$effect` to `$state`+`onMount`, per its own style suggestion).
  `src/pages/harness/[feature].astro` gained a `"help"` branch: imports `HelpHarness` + `buildHelp`/`getHelpSearch`/`getCmdline`/`getShell`; `getStaticPaths` now returns both `profile` and `help` entries under the fixture flag; props assembled the same way the real `/help` page does (`buildHelp(await getCollection("help"))`, `getHelpSearch()`, `getCmdline()`, `getShell()` — none of these are fixture-switched, same as profile's case).
  Page object `src/features/help/tests/ui/pages/HelpPage.ts` (PascalCase, getters, `getByTestId` only — playwright.md R002/R003). Its `openHarness()` needed a real hydration wait that profile's `ProfilePage` never needed: `client:load` ships JS asynchronously, and the server-rendered HTML (title/rows included) is present before any JS runs, so a spec pressing `?` immediately after `goto()` would race `<svelte:window>`'s listener attaching (this raced and failed 4/6 tests on the first run — see below). Fixed by adding a `data-testid="help-harness-ready"` element in `HelpHarness.svelte` whose `data-ready` attribute flips only after `onMount` (client-only, post-mount), and waiting on that attribute (`toHaveAttribute("data-ready", "true")`) rather than any content locator (which would resolve from the static markup alone, before hydration).
  Spec `src/features/help/tests/ui/harness/help.spec.ts` (7 tests): mount (title text, row count > 0, scope-tab count > 1), `?` opens the palette via the harness's own routing, the `"kil"` fuzzy canary (still valid post-step-6-revert — same unchanged scorer), Enter-on-keymap-row no-ops, Enter-on-command-row resolves and closes (asserted via a `help-harness-last-executed` testid rendered by the wrapper, since there's no real `executeSiteAction` to navigate through in isolation), Esc closes, and `status-bar-windows` count 0 (no kernel chrome).
  `playwright.config.ts` gained `help-harness` (testDir `src/features/help/tests/ui/harness`, one project at `viewports[0]` only, matching `profile-harness`'s shape). `package.json`'s `test:visual` gained the harness spec's path. `docs/testing/visual/running-tests.md` gained a `help-harness` row in the same commit.
  **Verified:** `pnpm check` 0 errors (118 files). Real build (`pnpm build`, dist wiped first): `find dist -iname "*harness*"` → only `dist/_astro/HelpHarness.<hash>.js` (998 bytes) — **no `dist/harness/` route directory**, and `grep -rl "HelpHarness" dist --include="*.html"` is empty (no real page links to it); confirmed this same chunk IS referenced once `pnpm build:fixtures` runs (`dist/harness/help/index.html`). This is a genuine finding, not a violation: Astro/Vite emits a JS chunk for every module reachable from a page's static import graph regardless of whether `getStaticPaths` renders a route for it, so a harness-only wrapper component (unlike profile's reused `Profile.svelte`, whose chunk always ships anyway) leaves an unreferenced but present chunk in the real build. D24(a)'s literal contract ("zero harness **pages**") holds — no route output exists — but phase 03's own verify wording ("`find dist -iname "*harness*"` empty") no longer returns fully empty once a feature needs a harness-only component; recorded as a clarifying finding in `Instructions/01-pre-phase/recipe-feature-map.md`'s D24 section for phases 05–11, flagged for an orchestrator ruling (bless the dead chunk as harmless, or track an env-gated-island cleanup for the post-migration hardening pass) rather than decided unilaterally here.
  Fixture build (`pnpm build:fixtures`, dist wiped first): `--project=help-harness` → **7/7 passed**. Full D20 re-run from this final state: real-build invocation (`smoke`+`legacy×2`+`common×2`+`profile×2`+`help×2`) → **1012/1012 passed**; fixture-build invocation (all `*-visual`/`identical.spec.ts` files + `profile-harness` + `help-harness`) → **60/60 passed** (the prior 53 + 7 new harness tests). `pnpm test:unit`: 339/339 passed. Golden parity re-checked: `diff -rq` still exactly the same 7 "Only in v1" files per viewport, zero `differ` lines. `public/generated/{grep-index,fs-index}.json` diffed programmatically — only the 3 new `src/` files added (`HelpHarness.svelte`, `help.spec.ts`, `HelpPage.ts`), nothing else; unrelated live-data drift reverted before committing.

## Acceptance criteria

Loop + swap + harness verified (or swap explicitly reverted-and-reported); verifier PASS + auditor clean on `src/features/help` (D23); commit discipline per 00-phases.md. Stop: no search-UX changes beyond what fuzzysort's scoring implies within the golden constraint.

## Results

All 7 steps complete. Steps 1–5 and 7 green; step 6 closed via revert-and-report, exactly as the plan's own D11 gate sanctions. See each step's own *Result* line above for full detail; summary for the orchestrator below.

**Commits (local, `frontend-rewrite`, in order):**
1. `99d2a54` — step 1: port `help`/`help-layout`/`help-search` specs + goldens + new visual spec/projects to `src/features/help`.
2. `9a908ec` — step 2: move `HelpView`/`HelpSearch`/`help-search`/`docline` and help content to their v2 homes.
3. (no commit) — step 3: full-gate re-verification, recorded in this file.
4. (no commit) — step 4: golden-parity re-verification, recorded in this file.
5. `acb3156` — step 5: move `help-search`/`docline` unit tests to their v2 homes.
6. `c14a375` — record steps 1–5 results + recipe-split section (documentation only).
7. `1f8ad0c` — record step 6's revert-and-report (documentation only; no source commit for step 6 itself, since everything was reverted).
8. `be78b6c` — step 7: add the help feature harness route, wrapper, page object, and spec.

**D16 rule-of-three verdict:** `docline.ts` → `src/common/lib/docline.ts`. Grepped real consumers (not comment mentions) — exactly 3, **none of them help itself**: `Shell.svelte` (shell-fs), `repositoriesState.svelte.ts` (repositories), `employmentRecordsState.svelte.ts` (employment); confirmed via `grep -n "docline" src/components/HelpView.svelte src/components/HelpSearch.svelte` returning nothing. A stronger rule-of-three case than profile's `resume.ts` (3 consumers vs. 2, and the module has zero coupling to its own "home" feature).

**D11 fuzzysort swap — reverted and reported, moved to Deferred:** installed `fuzzysort@4.0.2` (exact-pinned) and read its own `index.d.ts` rather than working from memory. An offline ranking-diff script against the REAL corpus (`cmdline.yaml` + `content/help/*.md` + `shell.yaml`, none fixture-switched — a valid proxy for what recipe `20-help-search` renders) showed 4 of 5 canaries reordering, most critically the golden recipe's own `"kil"` query: the old tiered cascade flattens `kill-window`/`kill-pane` into one tier (both "prefix, distance 0") and resolves the tie by corpus order (kill-window first); fuzzysort's continuous, length-sensitive scorer ranks `kill-pane` above `kill-window` regardless (confirmed even isolating the two bare labels: `0.8836` vs `0.8656`) — a structural mismatch (delegate.md's tie-break design assumes fuzzysort produces *equal* scores in these cases, and it essentially never does for different strings), not a tuning gap. Confirmed against the real pixel gate via a working-tree-only dress rehearsal (never committed): the swap made `20-help-search` fail at both viewports (7012/7000 px diff, confirmed via the diff image to be exactly the reordered results rows) while `11-help` stayed green; full revert (`git checkout --` + `pnpm install`) brought `help-visual` back to 4/4 green. No fuzzysort trace remains in any commit. Recommending `Instructions/00-phases.md`'s Deferred list gain: "helpSearch fuzzysort swap (D11) — reverted in phase 04, `20-help-search` ranks `kill-window` before `kill-pane` for `\"kil\"` under the old tiered cascade but fuzzysort's continuous scorer reorders them regardless of configuration; needs a deliberate goldens rebaseline (both viewports) in the same change that eventually lands it, not a cleverer fuzzysort config" — this file is the orchestrator's to edit, not touched here.

**D24 harness mechanism — reused verbatim, one genuine extension recorded:** help has no single top-level component (unlike profile), so its harness needed a new harness-only wrapper (`HelpHarness.svelte`, in the test tree, not the feature's real `components/`) implementing the minimal keydown split Terminal normally owns. This exposed a finding phases 02/03 never hit: Astro/Vite's real build still emits a JS chunk for a harness-only component (`dist/_astro/HelpHarness.<hash>.js`, 998 B) even though `getStaticPaths` renders no route for it — verified the chunk is genuinely dead in a real build (`grep -rl "HelpHarness" dist --include="*.html"` empty; only referenced once `build:fixtures` runs). D24(a)'s literal "zero harness pages" contract holds (no `dist/harness/` route directory in a real build), but phase 03's own verify command (`find dist -iname "*harness*"` empty) no longer returns fully empty. Recorded as a clarifying finding in `Instructions/01-pre-phase/recipe-feature-map.md`'s D24 section, flagged for phases 05–11 (any later harness-only wrapper will hit the same pattern) and for an orchestrator ruling — bless as harmless dead weight, or track an env-gated-island cleanup post-migration. Also hit a genuine hydration race profile never needed to handle (`client:load` ships JS async, but the server-rendered HTML is present immediately, so a spec pressing `?` right after `goto()` can beat `<svelte:window>`'s listener attaching) — fixed with an explicit `data-ready` hydration marker in the wrapper, waited on via `toHaveAttribute` rather than any content locator.

**Two common→feature edges, mechanical-only, flagged for the auditor (same channel/pattern as phases 02/03's own Meter/PaneTree findings — this is the third and fourth occurrence, exactly as phase 03 predicted would recur):**
- `src/common/components/PaneTree.svelte` now imports `../../features/help/components/HelpView.svelte` directly (already flagged twice before, for then-transient/other-feature components).
- `src/bootstrap/Terminal.svelte` now imports `../features/help/components/HelpSearch.svelte` directly — bootstrap→feature, which D24/D10 already sanction (bootstrap is allowed to know about every feature), so this one is *not* a new R004 violation, just noted for completeness.
Only import paths were updated; dependency direction pre-existed the move in both cases.

**Judgment calls, each preceded by an `advisor()` consult:** (1) the D16 docline.ts verdict; (2) whether to invest in tuning fuzzysort's configuration before concluding revert-and-report — advisor confirmed the label-only score evidence was structurally decisive and tuning would reimplement the deleted cascade; (3) the HelpHarness.svelte wrapper's design (mount both components, thin 3-way keydown split, live in the test tree) and its consequence (the dead-chunk finding) — advisor confirmed accept-and-flag over any redesign attempt (dynamic-import gating, a second harness route) that would exceed this phase's scope or re-litigate D24's settled mechanism.

**Nothing left red.** Zero golden churn (42 PNGs before and after, just 2 more redistributed into `src/features/help/tests/ui/visual/goldens/`). No POM refactor of the verbatim-ported `help*.spec.ts` files (D23(a) exemption, same as phase 03's `profile.spec.ts`). Ready for verifier/auditor — expect the auditor to review the two flagged common→feature edges, the dead-chunk finding, and (per D23(a)) the ported specs' locator style.

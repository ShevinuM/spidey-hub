# Phase 09 — repositories

> **Status: planned 2026-09-12, ready to execute. Predecessor phase 08 closed 2026-09-12.** Binding decisions: `Instructions/00-phases.md` (the only cross-phase document this plan may read). This plan is self-contained: every mechanism it needs is stated here verbatim, so the executor never reads another phase's folder.
>
> **This is the largest and highest-fallout phase of the migration.** Not because the component move is hard — it is eight files — but because repositories' *libraries* are consumed by `src/pages/*.astro` (×6), `src/bootstrap/Terminal.svelte`, `src/common/components/PaneTree.svelte`, the whole `src/common/components/editor/` tree, and phase 11's `Shell.svelte`/`shellIndex.ts`; because its *content tree* is read from disk by literal path by three files in other contexts; and because one already-authored **feature→feature** import edge (`repositoriesState` → notifications' `agoLabel`) becomes an architecture R004 violation the moment repositories lands in `src/features/`. §Background's rulings R1–R9 and the "Out-of-context literal-path breaks" table are the parts of this plan that earn its length. **Read both before step 1.**
>
> Carried-in item (phase-07 close, 2026-09-12): a single `legacy-1512x945 › repositories.spec.ts` failure surfaced once during phase 07's execution and did **not** reproduce in the verifier's subsequent clean 1012-pass real-build run; phase 08 saw one transient 6-failure run of the same spec, root-caused to build/revert-drift ordering rather than a real regression. **This phase watches for it**: if `repositories.spec.ts` proves genuinely flaky once repositories sits in its own context, root-cause it here rather than re-deferring. A failure that reproduces on a clean rebuild is a real defect; one that vanishes is a `dist/`-staleness artifact — rebuild before concluding either way.
>
> Carried-in item (phase-04 close audit, 2026-09-08): `common/tests/ui/pages/TerminalPage.ts:7` names a `RepositoriesPage` that does not exist yet — a pre-migration forward reference. Step 8's page object closes it by creating the file. **Verified 2026-09-12:** the mention is a page-object *naming-convention* example ("named for the UI surface it models … matching `RepositoriesPage`/`StatusBarPage`"), not an import — creating `src/features/repositories/tests/ui/pages/RepositoriesPage.ts` makes it true with no edit to `TerminalPage.ts` at all.

## Context

**`src/features/repositories`.** Everything below is scoped to it except the declared out-of-context wiring.

**Declared out-of-context wiring** (mechanical only — D24's closing clause; no behavioral edits outside the context, ever):

| file | why |
|---|---|
| `playwright.config.ts` | three project entries (§Mechanics 3) |
| `package.json` | `test:e2e` / `test:visual` explicit lists; `build:fixtures` **two** `cp` source paths (D22, R7) |
| `src/content.config.ts` | the `repositories` collection's `base:` ternary — **both** branches (D22) — plus its stale header prose (R8) |
| `src/common/lib/data.ts` | `repositories.yaml` `?raw` re-point (D17) + one stale comment |
| `src/common/lib/docline.ts` | one stale `fixtures/repositories/*.md` comment citation |
| `src/common/content/cmdline.yaml` | one stale `src/data/repositories.yaml` comment citation |
| `src/common/components/PaneTree.svelte` | `Repositories.svelte` import path + `Commit` type import (R5) |
| `src/common/components/editor/{Editor.svelte,editor-render.ts,editorState.svelte.ts}` | `TokenSpan` type import path (R3) |
| `src/bootstrap/Terminal.svelte` | `Commit` type import path (R2) |
| `src/pages/{index,repositories,profile,help,employment,retina-v}.astro` | `getCommitsByRepo` import path (R2) |
| `src/pages/harness/[feature].astro` | one `repositories` branch (D24) |
| `src/components/Shell.svelte`, `src/lib/shellIndex.ts`, `src/lib/shell.ts` | `repo-tree` import paths + stale comment citations (phase 11's tree; path-only) |
| `src/lib/githubTrees.ts` → `src/features/repositories/lib/github-trees.ts` | feature-owned lib move |
| `src/features/notifications/{lib/notification-store.ts,components/notificationsState.svelte.ts,tests/unit/notification-store.test.ts}` | `agoLabel` extraction (R1) — closed context, behavior-free |
| `common/tests/unit/views.test.ts` | `expectedView()`'s hardcoded path whitelist (**break #1**) |
| `common/tests/unit/docline.fixtures.json` | **four** real-fs `"path"` values (**break #2**) |
| `tests/e2e/grep.spec.ts` | one typed query + one literal `data-path` assertion (**break #3**) |
| `scripts/generate.mjs` | `srcDir` path constant (**break #5**) + four stale comment citations |
| `tests/visual/identical.spec.ts` | `SPLIT_OWNED_RECIPE_NAMES` gains three names; header comment |
| `tests/visual/adversarial-fixtures.spec.ts` | split and deleted (R6) — its employment half lands in a **closed** context |
| `docs/testing/{e2e,visual}/running-tests.md` | project rows + "not yet real" list (doc-practice, same commit) |
| `tests/visual/goldens/<vp>/{02-repositories,03-repositories-arrow,12-all-projects}.png` | deleted after the copies are verified |

**Explicitly NOT touched by this phase** — verified, not assumed. If you believe one needs editing, **stop and report**:

- `repos/` (8 checked-out repos at the repo root) — `generate.mjs` input, not source. **Stays at root.** `repositories.spec.ts` reads `join(ROOT, "repos", …)`; only its `ROOT` re-depths.
- `src/generated/commits/` and `src/generated/file-icons.json` — generated artifacts written by `generate.mjs`. **Never move.** `commits.ts`'s `REAL_GLOB` only re-depths.
- `public/generated/**` — build output. `generate.mjs`'s `outDir` constants, `OWNER`, the `REPOS` list, `GREP_ROOT_SUBDIRS`, `FS_INDEX_SUBDIRS` are all **unchanged** (`fixtures/` still exists after this phase — `grep-index.json` and `fs-index.json` remain, phases 10 and 11).
- `public/generated/contributions.json` — a *different*, generated artifact written by `generateContributions()`. Only `fixtures/contributions.json` moves (R7).
- `tests/unit/keyframes-dedup.test.ts` — **stays in `tests/unit/`** (R9).
- `src/common/lib/views.ts` — `grepPathToView()`'s regexes are segment-anchored and depth-agnostic; they keep working unchanged **provided the content tree keeps its `content/repositories/` segment pair** (R4). Its line-100 docstring stays accurate. **Zero edits.**
- `src/pages/repositories.astro`'s body — apart from the one `getCommitsByRepo` import path, it imports only from `common/lib/data` and `bootstrap/`. No other edit.
- `src/common/components/editor/` beyond the three `TokenSpan` import lines — the editor is common's (D9).
- `common/tests/ui/pages/TerminalPage.ts` — creating `RepositoriesPage` closes its forward reference; the file itself needs no edit.

## Objective

The full Repositories feature — panels, repo browse, file preview highlighting, commits, status dots — lives in `src/features/repositories/`, moved verbatim, with all four e2e specs and the `02-repositories` / `03-repositories-arrow` / `12-all-projects` goldens green at both viewports and zero golden churn; its shared libraries are promoted to `common/lib/` per D16 so no feature→feature or newly-authored common→feature *value* edge is created; the generate-time-only client-bundle boundary for `highlight.ts` is preserved and proven; the three fixture mechanisms move with their build wiring updated in the same change; and a `repositories-harness` route mounts `Repositories` with no Terminal kernel.

## Background — verified inventory

Every path, line number and count below was verified on disk on 2026-09-12. **Do not re-derive; do verify before editing** — grep for the symbol, do not trust a line number.

### Move table

| from | to | lines |
|---|---|---|
| `src/components/repositories/Repositories.svelte` | `src/features/repositories/components/Repositories.svelte` | 208 |
| `src/components/repositories/RepositoriesPanel.svelte` | `…/components/RepositoriesPanel.svelte` | 43 |
| `src/components/repositories/ReposPanel.svelte` | `…/components/ReposPanel.svelte` | 120 |
| `src/components/repositories/FilesPanel.svelte` | `…/components/FilesPanel.svelte` | 140 |
| `src/components/repositories/CommitsPanel.svelte` | `…/components/CommitsPanel.svelte` | 120 |
| `src/components/repositories/PreviewPanel.svelte` | `…/components/PreviewPanel.svelte` | 64 |
| `src/components/repositories/StatusPanel.svelte` | `…/components/StatusPanel.svelte` | 78 |
| `src/components/repositories/repositoriesState.svelte.ts` | `…/components/repositoriesState.svelte.ts` (D15: state classes keep `<name>State.svelte.ts`) | 701 |
| `src/lib/githubCommits.ts` | `src/features/repositories/lib/github-commits.ts` (D15 kebab) | 103 |
| `src/lib/githubTrees.ts` | `…/lib/github-trees.ts` (D15 kebab) | 210 |
| **`src/lib/commits.ts`** | **`src/common/lib/commits.ts`** (D16 promotion — R2) | 106 |
| **`src/lib/repoTree.ts`** | **`src/common/lib/repo-tree.ts`** (D16 promotion — R3) | 190 |
| **`src/lib/highlight.ts`** | **`src/common/lib/highlight.ts`** (D16 promotion — R3) | 225 |
| **`agoLabel` (11 lines, inside `src/features/notifications/lib/notification-store.ts`)** | **`src/common/lib/ago-label.ts`** (D16 promotion — R1) | 11 |
| `src/content/repositories/` (8 `.md`, flat) | **`src/features/repositories/content/repositories/`** — the nested `repositories/` segment is load-bearing (R4) | 8 files |
| `src/data/repositories.yaml` | `src/features/repositories/content/repositories.yaml` (D17) | 93+ |
| `fixtures/repositories/` (4 `.md`, flat) | `src/features/repositories/tests/ui/support/repositories/` | 4 files |
| `fixtures/repos/` (`all-projects.json`, `webbing-lab.json`) | `src/features/repositories/tests/ui/support/repos/` | 2 files |
| `fixtures/commits/` (4 `.json`) | `src/features/repositories/tests/ui/support/commits/` | 4 files |
| `fixtures/contributions.json` | `src/features/repositories/tests/ui/support/contributions.json` (D7 correction — R7) | 1 file |
| `tests/e2e/repositories.spec.ts` | `src/features/repositories/tests/ui/e2e/repositories.spec.ts` | 846 |
| `tests/e2e/repositories-preview-highlight.spec.ts` | `…/tests/ui/e2e/repositories-preview-highlight.spec.ts` | 96 |
| `tests/e2e/repositories-preview-scroll.spec.ts` | `…/tests/ui/e2e/repositories-preview-scroll.spec.ts` | 70 |
| `tests/e2e/repositories-status-dots.spec.ts` | `…/tests/ui/e2e/repositories-status-dots.spec.ts` | 147 |
| `tests/unit/githubCommits.test.ts` | `src/features/repositories/tests/unit/github-commits.test.ts` | — |
| `tests/unit/githubTrees.test.ts` | `…/tests/unit/github-trees.test.ts` | — |
| `tests/unit/all-projects-fixture.test.ts` | `…/tests/unit/all-projects-fixture.test.ts` | — |
| `tests/unit/repoTree.test.ts` | **`common/tests/unit/repo-tree.test.ts`** (follows its module — R3) | — |
| `tests/unit/highlight.test.ts` | **`common/tests/unit/highlight.test.ts`** (follows its module — R3) | — |
| the two `agoLabel` tests inside `notification-store.test.ts` | **`common/tests/unit/ago-label.test.ts`** (new file, tests moved verbatim — R1) | 2 tests |
| `tests/visual/goldens/<vp>/{02-repositories,03-repositories-arrow,12-all-projects}.png` | `src/features/repositories/tests/ui/visual/goldens/<vp>/` | 6 files |
| `tests/visual/adversarial-fixtures.spec.ts` (repositories half) | `src/features/repositories/tests/ui/visual/adversarial-fixtures.spec.ts` (R6) | 2 tests |
| `tests/visual/adversarial-fixtures.spec.ts` (employment half) | `src/features/employment/tests/ui/visual/adversarial-fixtures.spec.ts` (R6) | 2 tests |

`src/components/repositories/` holds exactly those eight files and nothing else — verified; the directory is deleted when empty. After this phase `src/components/` holds only `Shell.svelte` and `grep-overlay/`, and `src/lib/` holds only `grep.ts`, `shell.ts`, `shellIndex.ts` (phases 10 and 11).

### Orchestrator rulings (do not re-litigate — architecture R003)

- **R1 — `agoLabel` is extracted from notifications' `notification-store.ts` to `src/common/lib/ago-label.ts`. This is mandatory, not optional.** `src/components/repositories/repositoriesState.svelte.ts:24` imports `agoLabel` from `../../features/notifications/lib/notification-store`. Today that is a *legacy-tree → feature* edge, which is the direction the whole migration moves in. The moment step 5 lands the state class in `src/features/repositories/`, it becomes **feature → feature** — the one dependency `docs/checklist/general/architecture.md` R004 forbids outright, and the exact edge the D7 dashboard-leg correction was written to avoid. D16's bar is "≥2 verified real consumers (grep before promoting)"; the grep was run and found **two** real consumers — `src/features/notifications/components/notificationsState.svelte.ts:229` and `repositoriesState.svelte.ts:114` — so D16 mandates promotion. The function is **11 lines, pure, and has zero imports**; this is not "a function inside a 431-line module being moved verbatim" (phase 06's declining rationale, which applied when repositories was still legacy and the edge was therefore legal) — it is a self-contained helper with a clean surface, exactly what D16 is about. **Mechanics:** create `src/common/lib/ago-label.ts` holding the function and its docstring verbatim; delete it from `notification-store.ts` (no re-export shim); update `notificationsState.svelte.ts`'s import list, `repositoriesState.svelte.ts`'s import, and `notification-store.test.ts`'s import list. Move the **two** `agoLabel` tests out of `src/features/notifications/tests/unit/notification-store.test.ts` into a new `common/tests/unit/ago-label.test.ts` — verbatim, same assertions, only the import path changes. Editing a closed phase's context is sanctioned here on the same grounds D23 already sanctions test-infra fixes and the phase-07 tracker move into common: behavior-free, mandated by a binding rule, and cheaper now than after the violation is committed. **Own commit, before the feature move.** Notifications' own gates (`notifications-*`, `notifications-harness`, unit) must be green afterwards — a red notifications suite means the extraction was not verbatim.
- **R2 — `src/lib/commits.ts` is promoted to `src/common/lib/commits.ts`, NOT moved into the feature.** Verified consumer graph outside `src/components/repositories/`: `getCommitsByRepo` (a **value** import) in six route pages — `src/pages/{index,repositories,profile,help,employment,retina-v}.astro`; `type Commit` in `src/bootstrap/Terminal.svelte:43` and `src/common/components/PaneTree.svelte:37`; `type Commit` in `src/lib/githubCommits.ts:13`. D16's bar is met many times over. Decisive beyond the bar: this module is a **server-only, build-time data loader whose output is threaded to the feature as a plain prop** — its own header says "Server-only, same as src/common/lib/data.ts: this module must never be imported from a Svelte island" — which is precisely the role `common/lib/data.ts` already plays for every feature's content (`getPersonnel()`, `getDashboard()`, `getRepositories()`, all called in page frontmatter and passed down through `Terminal.svelte` → `PaneTree.svelte`). Homing it in the feature would make six ordinary route pages import a **feature** module by value — a new edge class `architecture.md` R008 forbids more squarely than anything else on the table, and one no later phase would undo. **Mechanics:** `REAL_GLOB`'s `../generated/commits/*.json` and `FIXTURE_GLOB`'s `../../fixtures/commits/*.json` both re-depth — **verify both with `path.relative`, do not guess**; `src/generated/commits/` does not move, the fixture commits do (step 5). The dead-but-exported `commitsDir` string constant's two literals go stale on the move: **update the literals to the new real paths** (comments.md R003 / truthfulness); do **not** delete the export — that is the rewrite the stop conditions forbid. Record the dead export as a hardening-pass item in Results.
- **R3 — `src/lib/repoTree.ts` → `src/common/lib/repo-tree.ts` and `src/lib/highlight.ts` → `src/common/lib/highlight.ts`, both promoted, in that dependency order.** `repoTree.ts` verified consumers outside repositories: `src/components/Shell.svelte:45` imports the **value** `repoFileText` (Shell lands in `src/features/shell-fs` in phase 11 — leaving `repo-tree` in repositories would author a feature→feature edge then, R004 again), `src/lib/shellIndex.ts:17` imports `RepoFile`/`RepoIndex` types (phase 11 likewise), and all three of `src/common/components/editor/{Editor.svelte,editor-render.ts,editorState.svelte.ts}` import `TokenSpan` from it. D16's bar is met. `highlight.ts` follows it because `repoTree.ts:17` does `import type { TokenSpan } from "./highlight"` and `repoTree.ts:19` **re-exports** it (`export type { TokenSpan }`) — `TokenSpan` is defined at `highlight.ts:13` and is the *editor's* line-token format, consumed by three common files through that re-export chain. Leaving `highlight.ts` in the feature would author a **common→feature** edge for a type common owns the rendering of. Its other consumer is `scripts/generate.mjs:51` (out-of-context build tooling, which keeps its explicit `.ts` extension — D23(d): Node's ESM resolver requires it). **The stub's "iff only generate.mjs + repositories consumers" condition is therefore falsified and does not fire.** **The generate-time-only client-bundle boundary is preserved by import *kind*, not by location** — `repo-tree.ts`'s import of `highlight` is `import type`, fully erased under `verbatimModuleSyntax: true`, so no runtime chain reaches Shiki. That property is **not assumed, it is gated**: capture `grep -rlE "createHighlighterCore|shikijs" dist --include="*.js"` on the pre-move build (expected: empty) and re-run it on every post-move build. A non-empty result is a stop-and-report, not something to work around. Whether a Shiki tokenizer belongs in `common/lib/` long-term is a hardening-pass question; it is not re-litigated here.
- **R4 — the content tree moves to `src/features/repositories/content/repositories/`, keeping the `content/repositories/` segment pair. This is load-bearing, not cosmetic.** `src/common/lib/views.ts`'s `grepPathToView()` routes a grep hit with `/(^|\/)content\/repositories\/|(^|\/)Repositories\.svelte$/` — segment-anchored and depth-agnostic, exactly like the `content/personnel/` rule phase 08 relied on. Flattening the 8 `.md` files to `src/features/repositories/content/*.md` (which the 2026-08-29 stub's move table implied) would silently stop routing every repositories content hit in the grep overlay — a **behavior change with no failing compile**, caught only by `views.test.ts`'s whitelist. Employment set the precedent: `src/features/employment/content/personnel/` (dir) alongside `content/personnel.yaml` (file). Repositories mirrors it exactly: `content/repositories/` (dir) alongside `content/repositories.yaml` (file). The two coexist. The fixture tree at `…/tests/ui/support/repositories/` deliberately does **not** carry a `content/` segment, so it stays unrouted (`null`) once it enters the grep index — same as employment's fixture personnel tree, verified live.
- **R5 — `PaneTree.svelte` → `Repositories.svelte` is a path-only update, recorded as D23(c).** Same class as the `PaneTree.svelte`→`Profile.svelte` edge D23(c) names verbatim and the `→Dashboard.svelte` / `→EmploymentRecords.svelte` edges phases 07 and 08 recorded. Direction pre-existed the move. **No re-export shim, no stub, no injection registry** — that registry is a Deferred hardening item. `PaneTree.svelte`'s `type Commit` import becomes `../lib/commits` (intra-common) once R2 lands, so that half stops being a cross-context edge at all.
- **R6 — `tests/visual/adversarial-fixtures.spec.ts` is split in two and the root file deleted. Phase 08's R2 deferred this decision to this phase; this is the decision.** The file holds two independent `describe` blocks — an employment half (2 tests, the ~300-line `evidence-cataloguer` record's scroll/wrap behavior) and a repositories half (2 tests, `flerken-watch.md`'s 400-char line wrapping in the Content panel, and `webbing-lab`'s empty-repository path). Both halves' contexts now exist. Its header and three in-body comments cite `fixtures/personnel/`, `fixtures/repositories/*.md`, `fixtures/repos/webbing-lab.json` — paths this phase invalidates — so the file must be edited this phase regardless; splitting it costs the same edit. Employment's half lands at `src/features/employment/tests/ui/visual/adversarial-fixtures.spec.ts`: a **test-infra-only, behavior-free** addition to a closed context, the same class D23 already sanctions. **Placement is deliberate**: these are fixture-build tests with no goldens, and `tests/ui/visual/` is the only per-feature folder whose Playwright projects run on the D20(b) fixture build. Under `<feature>-visual-<viewport>` they run at both viewports — which is exactly what they do today under the root `1512x945`/`1920x1080` projects, so the test count is preserved at 8, not doubled. The three shared helpers (`gotoReady`, `prefixDigit`, `fitsWithin`) are **duplicated into both halves verbatim**; extracting them into `common/tests/ui/support/` is the rewrite-the-safety-net hazard D23(a) exists to prevent (phase 05's `terminalReady()` ruling, `e2e-testing.md` R009), and closes in the deferred POM pass. Record the duplication in Results so the auditor sees it declared. Both halves keep their `BOOT_SEEN_STORAGE_KEY` import (re-depthed, extension stripped per D23(d)) and their raw-CSS locators untouched (D23(a)).
- **R7 — the fixture wiring is THREE distinct mechanisms; do not conflate them, and do not invent a `cp` clause.** (a) `fixtures/repositories/*.md` is the **content collection's fixture branch** — `src/content.config.ts`'s `base:` ternary only; consumed at Astro build time, nothing to copy into `dist/`. **Both** branches update: `useFixtures ? "fixtures/repositories"` → `"src/features/repositories/tests/ui/support/repositories"`, and `: "src/content/repositories"` → `"src/features/repositories/content/repositories"`. (b) `fixtures/commits/*.json` is an **`import.meta.glob` build input** of `commits.ts` — no `cp` clause exists or is needed; only the glob literal changes (R2). (c) `fixtures/repos/*.json` and `fixtures/contributions.json` are **static JSON assets fetched client-side**, each with its own `cp` clause in `package.json`'s `build:fixtures`: `cp fixtures/repos/*.json dist/generated/repos/` and `cp fixtures/contributions.json dist/generated/contributions.json` — **both source paths update, both `dist/generated/` destinations stay exactly as they are.** `fixtures/contributions.json` is here by the **D7 orchestrator correction of 2026-09-12** (recorded in `00-phases.md`): D7's original dashboard leg was falsified by the code — dashboard has zero consumers, while this context has two (`repositoriesState.svelte.ts`'s runtime `fetch("/generated/contributions.json")` and `StatusPanel.svelte`'s contribution grid). Phase 07 deliberately left the file and its `cp` clause untouched. **Do not assume it already moved — verify it is still at `fixtures/contributions.json` before moving it.** `public/generated/contributions.json` is a *different*, generated artifact written by `generateContributions()` in `scripts/generate.mjs`; it never moves and `generate.mjs`'s output constants never change. Only (c)'s two `cp` sources touch `build:fixtures`; `grep-index.json` and `fs-index.json` clauses stay untouched (phases 10 and 11).
- **R8 — `src/content.config.ts`'s stale header prose is fixed in this phase's ternary commit.** `00-phases.md`'s Deferred list assigns it to "whichever phase next edits `content.config.ts`", which is this one. Two blocks: lines ~3–8 describe the `repositories` fixture switch and cite `src/lib/commits.ts` (both invalidated here), and lines ~10–17 still describe `fixtures/personnel/` / `src/content/personnel/` and cite "Phase 7b.2" (phase 08's known remainder). Fix **both**, plus the `generateId` comment's `fixtures/repositories/*.md` / `src/content/repositories/*.md` citation. Rewrite in the file's own terms; the "Phase 7b.2" citation is a v1 process artifact (comments.md R008) and goes away entirely rather than being repointed.
- **R9 — `tests/unit/keyframes-dedup.test.ts` is NOT repositories' and stays in `tests/unit/`.** The stub asked for this to be read and ruled; it was. The test scans the **entire `src/` tree** for duplicate `@keyframes` declarations — a repo-wide anti-drift fitness test whose only connection to repositories is a historical sentence in its header naming the two dead `ReposPanel.svelte`/`StatusPanel.svelte` `pls` duplicates that motivated it. It asserts nothing about repositories, imports nothing from it, and reads no repositories path. Moving it into a feature would scope a whole-tree invariant to one context. It stays out of context, alongside `freePort.test.ts`, until the `tests/audits/` boundary-fitness pass (architecture R009, Deferred) gives it a proper home. Its own `PLAN.md F1 (Iteration 7, Phase 1)` header citation is the already-recorded Deferred comments.md R008 item for out-of-context files — **not this phase's to fix**; leave it and say so in Results.
- **R10 — `repositoriesState.svelte.ts` is covered by D23(e) in advance, twice over.** `00-phases.md`'s phase-06 ruling generalized (e) to **any `<name>State.svelte.ts` relocated verbatim** (classes.md encapsulation/ordering: `private` rather than `#` fields, public mutable `$state` written by sibling components, mid-file field declarations); its phase-08 rulings added `handleKey(e): boolean` (classes.md R002 — the boolean is `Terminal.svelte`'s delegation contract, `if (ref.handleKey(e)) return`, which `Repositories.svelte:91` implements identically) and files-and-naming R011 (pure helpers at module scope inside a state-class file), with the phase-08 text explicitly stating "**Phases 09–11 will each hit this** … so no re-ruling is needed per phase". At 701 lines this is the largest state class in the migration; expect a long list of these findings and expect every one of them to be exempt. **Do not "fix" them.** Code *authored* this phase is never covered.

### Out-of-context literal-path breaks — the part that bites

These read repositories' trees **from disk by literal path** (or name a moved file in a path constant). Most import nothing from repositories, so an import-only sweep misses them. Each is fixed **in the same change** as the move that invalidates it (D8), and each is declared out-of-context wiring.

**Break #1 — `common/tests/unit/views.test.ts` (fails as a whitelist mismatch, 2 tests).** `expectedView()` hand-audits *every path in the real generated `public/generated/grep-index.json`* against hardcoded prefixes:
```ts
if (path.startsWith("src/content/repositories/")) return "repositories";          // line ~30
if (path === "src/components/repositories/Repositories.svelte") return "repositories";  // line ~32
```
After the move + `pnpm generate` the real index carries `src/features/repositories/content/repositories/…` and `src/features/repositories/components/Repositories.svelte`. `grepPathToView()` itself still classifies both correctly (R4), but these literals will not match. **The two lines are invalidated by two different moves and are therefore fixed in two different commits (D8): the exact-match component literal in step 4, with the component move; the content-prefix literal in step 5, with the content move.** The index **also gains** `src/features/repositories/tests/ui/support/repositories/*.md` entries (they move from `fixtures/`, which is not grep-indexed, into `src/`, which is) — those must map to `null`, which they do, exactly as employment's fixture personnel tree already does. The `routed.length >= 8` assertion is unaffected: the routed **count** is unchanged, only the strings. The prototype-fixture literals lower in the file (`src/components/Tracker.svelte`, `src/content/xp/…`) are deliberately fake and are **not** touched.

**Break #2 — `common/tests/unit/docline.fixtures.json` (fails as `ENOENT`, four paths).** Lines 18, 36, 55 and 77 carry `"path"` values `fixtures/repositories/{shevinum-dev,spider-tracker,flerken-watch,latveria-atlas}.md`, and `common/tests/unit/docline.test.ts` does a **real `readFileSync`** on each. The moment the fixture tree moves these throw. **This is the single most easily-missed break in the phase** — it is a `.json` fixture, so it matches no `.ts`/`.svelte` grep. (Phase 08 hit the same file for its three personnel paths; those are already correct and are not touched.)

**Break #3 — `tests/e2e/grep.spec.ts` (fails as a wrong assertion, two lines + a comment). Fixed in step 4, with the component move that invalidates it.** Line ~144 types the literal `"src/components/repositories/Repositories.svelte"` into the grep overlay and line ~145 asserts a row's `data-path` equals it; the line-137 comment names the same path. The real grep index will no longer contain it. `grep.spec.ts` belongs to phase 10's not-yet-migrated context; D8 and D24's mechanical-edit clause sanction updating it here — **those lines only**, nothing else in that file.

**Break #4 — `tests/unit/all-projects-fixture.test.ts` (fails as `ENOENT`, and it is itself moving).** `ROOT = join(import.meta.dirname, "../..")`, `FIXTURES_PROJECTS_DIR = join(ROOT, "fixtures/repositories")`, `FIXTURES_ALL_PROJECTS_JSON = join(ROOT, "fixtures/repos/all-projects.json")`, plus five message/comment strings naming those paths. All re-depth **and** re-path when the test lands at `src/features/repositories/tests/unit/` (ROOT becomes `../../../../..` — verify, do not guess). The test's whole purpose is proving the committed `all-projects.json` matches an index freshly built from the fixture `.md` files, so a wrong path here fails loudly rather than silently — good.

**Break #5 — `scripts/generate.mjs` (fails as a skipped artifact, not a throw — the dangerous one).** Line ~237: `const srcDir = join(ROOT, "src/content/repositories");`. Line ~250 has a `console.warn("[generate] src/content/repositories not found — skipping all-projects.json.")` **keep-existing fallback** — so a missed repoint does **not** throw; it silently stops regenerating `public/generated/repos/all-projects.json`. Update the constant and the warn string. Four comment citations in the same file (lines ~9, ~12, ~17, ~27, ~36, ~229, ~233, ~318) name `src/content/repositories`, `src/lib/{githubTrees,repoTree,highlight}.ts` and `src/components/repositories/repositoriesState.svelte.ts` — repoint all of them in the same change (comments.md R003). The `import` at line 51 (`../src/lib/highlight.ts`) is a **value** import and must be repointed to `../src/common/lib/highlight.ts`, keeping its explicit `.ts` extension (D23(d)).

**Break #6 — the build/data wiring.** `src/content.config.ts`'s ternary (both branches, R7a) and header (R8); `src/common/lib/data.ts:25`'s `import repositoriesRaw from "../../data/repositories.yaml?raw"` → `"../../features/repositories/content/repositories.yaml?raw"` plus its line-332 comment naming `fixtures/repos/all-projects.json`; `package.json`'s two `build:fixtures` `cp` sources (R7c); `src/common/lib/docline.ts:64` and `src/common/content/cmdline.yaml:116` comment citations.

After step 5, run these to prove none were missed:
```bash
grep -rn "content/repositories\|components/repositories\|data/repositories\.yaml\|fixtures/repos\|fixtures/commits\|fixtures/repositories\|fixtures/contributions" src common tests scripts docs package.json | grep -v "^src/features/repositories"
grep -rn "lib/repoTree\|lib/commits\|lib/highlight\|lib/githubCommits\|lib/githubTrees\|notification-store\"" src common tests scripts docs | grep -v "^src/features/repositories"
```
Every surviving hit must be an intentionally-updated line.

### Other verified facts

- **v1 drift: nothing is genuinely diverged.** `src/lib/{repoTree,githubCommits,githubTrees,highlight}.ts`, `src/data/repositories.yaml`, all of `src/content/repositories/`, `fixtures/{repositories,repos,commits}/` and `fixtures/contributions.json` are **byte-identical** to the v1 working tree. The eight components differ only by `../../lib/data` → `../../common/lib/data` and `../PanelBadge.svelte` → `../../common/components/PanelBadge.svelte` path rewrites from earlier phases; `commits.ts` differs by one comment path; the four e2e specs differ only by their `fixtures` import path; the five unit tests differ only by the D3 `node:test` → Vitest port. Every golden is byte-identical to v1's.
- **All four ported e2e specs import the shared fixture** (`../../common/tests/ui/support/fixtures.ts`) with an explicit `.ts` extension the port step must strip (D23(d)). Only `repositories.spec.ts` touches the filesystem: `ROOT = join(import.meta.dirname, "../..")` feeding `join(ROOT, "repos", …)` and `join(ROOT, "src/generated/commits", …)`. **Both targets stay at the repo root**, so only `ROOT`'s depth changes — no content-path break inside the specs. Two of them cite `src/lib/highlight.ts` in comments; repoint.
- **Recipe ownership: three recipes, six goldens.** `02-repositories` and `03-repositories-arrow` live in `recipes`; `12-all-projects` lives in `extraRecipes` — the same two-array shape help already handles (`11-help` in `extraRecipes`, `20-help-search` in `iteration3Recipes`), so `src/features/help/tests/ui/visual/identical.spec.ts` is the model to copy. Verified `find tests/visual/goldens -iname "*repositor*" -o -iname "*all-projects*"` returns exactly those three names × two viewports.
- **Post-phase-08 shared state** (this is what you are appending to): `SPLIT_OWNED_RECIPE_NAMES` holds **14** entries and gains three. `test:e2e`'s project list ends `--project=employment-1512x945 --project=employment-1920x1080`. `test:visual`'s path list ends with employment's `identical.spec.ts` and `harness/employment.spec.ts`, and **begins** with `tests/visual/identical.spec.ts tests/visual/adversarial-fixtures.spec.ts` — the second of those is removed by R6 and replaced by two feature paths. `src/pages/harness/[feature].astro` has `profile`, `help`, `boot`, `notifications`, `dashboard`, `employment` branches. Goldens are distributed root 14 / common 10 / features 18 = 42; after this phase root drops to **4 recipes / 8 PNGs**, repositories gains **3 recipes / 6 PNGs**, total still 42.
- **Unit baseline: 346 passed / 20 files** (re-measured 2026-09-12). This phase moves five unit files and creates one (`ago-label.test.ts`, holding two tests relocated out of `notification-store.test.ts`), so the end state is **346 passed / 21 files** — the *test* count is unchanged because nothing is written or deleted, only relocated. A changed test count means something was lost or duplicated. Breaks #1, #2 and #4 all live in unit tests, so a red `test:unit` here means a missed literal path, not a flaky suite.
- **`vitest.config.ts` already globs all three homes** (`tests/unit/`, `common/tests/unit/`, `src/features/*/tests/unit/`). **No edit.**
- **`Repositories.svelte` exports `handleKey(e): boolean`, `isEditorOpen()` and `runEditorExCommand(cmd)` and attaches no listener of its own** — `Terminal.svelte` owns the window keydown listener and routes through `PaneTree`'s shared `refs` map. Harness wrapper required (R11 in §Mechanics 5).
- **`repositoriesState.svelte.ts`'s import surface** (all of it, verified): `astro:content` (type), `common/lib/data` (type), `../../lib/commits` (type — becomes `common/lib/commits` at step 4), `svelte`'s `untrack`, `common/lib/docline`, `../../lib/repoTree` (becomes `common/lib/repo-tree` at step 4), `../../lib/githubCommits` and `../../lib/githubTrees` (become sibling feature libs), `common/lib/file-icons` (via `FilesPanel.svelte`), and `agoLabel` (R1). After R1–R3 land, **every one of its imports is either `common/…` or a feature sibling** — no legacy-tree import survives.
- **The `fixtures/` directory survives this phase.** `grep-index.json` (phase 10) and `fs-index.json` (phase 11) remain, so `generate.mjs`'s `FS_INDEX_SUBDIRS` (which lists `fixtures`) needs no edit. Expect large but fully-accountable `fs-index.json` churn: ~11 files leave `fixtures/…` and reappear under `src/features/repositories/…`.

### Stale-comment debt (fix in the same change as the move that invalidates it)

Beyond the break table: `src/lib/shell.ts` (lines ~69, ~123, ~275), `src/lib/shellIndex.ts` (lines ~21, ~79) and `src/lib/githubTrees.ts:21` cite `src/lib/repoTree.ts`; `src/components/repositories/Repositories.svelte:24` cites `src/lib/githubTrees.ts`; `src/lib/commits.ts`'s header cites `src/content/repositories/*.md`, `fixtures/repositories/*.md`, `fixtures/commits/`, `src/lib/githubTrees.ts`, `src/lib/githubCommits.ts` and `src/pages/repositories.astro`; `tests/unit/highlight.test.ts`'s header cites `src/lib/highlight.ts`. All repoint to the new homes. **A repoint is not automatically a fix**: verify the new path exists on disk before writing it, and when rewriting a comment block re-read **every** path it mentions, not only the clause that motivated the edit — phase 08 had two stale paths survive a sweep commit that hand-edited their own paragraph.

## Mechanics (settled by phases 02–08; reuse verbatim, do not redesign — architecture R003)

**1. D21(a) snapshot paths.** The root `snapshotPathTemplate` stays `"tests/visual/goldens/{projectName}/{arg}{ext}"`. Every split visual project carries its **own** template with the viewport as a **literal**, never `{projectName}`.

**2. Project naming.** A Playwright project is 1:1 with one `use` config, so each tier emits **two** entries via `viewports.map(...)`. **There is no bare alias and no glob** — `--project=repositories` and `--project="*-visual"` both error. Always the two explicit flags.

**3. The three project entries this phase adds** to `playwright.config.ts`, appended after the `employment-harness` entry:

```ts
...viewports.map((viewport) => ({
  name: `repositories-${viewport.name}`,
  testDir: "./src/features/repositories/tests/ui/e2e",
  use: { ...devices["Desktop Chrome"], viewport: { width: viewport.width, height: viewport.height }, deviceScaleFactor: 1 },
})),
...viewports.map((viewport) => ({
  name: `repositories-visual-${viewport.name}`,
  testDir: "./src/features/repositories/tests/ui/visual",
  snapshotPathTemplate: `src/features/repositories/tests/ui/visual/goldens/${viewport.name}/{arg}{ext}`,
  use: { ...devices["Desktop Chrome"], viewport: { width: viewport.width, height: viewport.height }, deviceScaleFactor: 1 },
})),
{
  name: "repositories-harness",
  testDir: "./src/features/repositories/tests/ui/harness",
  use: { ...devices["Desktop Chrome"], viewport: { width: viewports[0].width, height: viewports[0].height }, deviceScaleFactor: 1 },
},
```

**4. D24 harness route.** `src/pages/harness/[feature].astro` is one shared dynamic route gated by `if (process.env.PORTFOLIO_FIXTURES !== "1") return [];` in `getStaticPaths`. Add a `repositories` entry to (a) the `getStaticPaths` array, (b) the prop-assembly lines building the same props the real page does — `getRepositories()` from `common/lib/data`, `await getCollection("repositories")`, and `getCommitsByRepo(projects)` from `common/lib/commits` (R2 makes this a `common/` import, same as everything else that file imports) — and (c) a conditional block mounting `RepositoriesHarness`. The collection fixture-switches itself under `PORTFOLIO_FIXTURES=1`, so no special fixture handling is needed.

**5. Harness wrapper + hydration race — both required.** `Repositories.svelte` exports `handleKey()` but attaches no listener, so the wrapper reproduces **only** that keydown→`handleKey()` routing, exactly as `NotificationsHarness.svelte` and `EmploymentHarness.svelte` already do (`bind:this={ref}` + `<svelte:window onkeydown={(e) => ref?.handleKey(e)} />`), and documents the divergence in its header comment. `isFocused={true}` is hardcoded (only mounted instance). Because the harness ships `client:load`, render an `onMount`-flipped element and wait on **that**:

```svelte
<div data-testid="repositories-harness-ready" data-ready={ready}></div>
```

Repositories mounts a live-commit refresh that hits `api.github.com` — the harness spec must `page.route("**/api.github.com/**", (route) => route.abort())` in a `beforeEach`, same as every `identical.spec.ts` already does, or it races the network.

**5b. Harness assertions are web-first — do not copy the ported specs' reads.** Phase 05 lost a fix round to this. A verbatim-ported spec may read values non-retryingly (`textContent()`, `page.evaluate()`) because it races a *live* clock. A harness spec does not inherit that justification. Assert through the page object with `await expect(pageObject.x).toHaveText(...)` / `.toHaveAttribute(...)`. **`playwright.md` R002 bans CSS selectors unconditionally — that includes `document.querySelector` inside `page.evaluate()`.** The one sanctioned non-retrying read is deriving an *expected value* to feed a web-first assertion — never as the assertion itself.

**6. Page objects.** `src/features/repositories/tests/ui/pages/RepositoriesPage.ts`, a sibling of `e2e/`/`visual/`/`harness/` — this file is what closes `TerminalPage.ts:7`'s forward reference. A feature page object **may not** redefine kernel-chrome locators — compose the shared class:

```ts
import { StatusBarPage } from "../../../../../../common/tests/ui/pages/StatusBarPage";
```

Verify that relative depth with `node -e "…path.relative…"`; do not guess it.

**7. Locators.** `playwright.md` R002 bans CSS selectors unconditionally, even scoped off a resolved testid. If a repeated element needs narrowing, add a real `data-testid` to the markup (attribute-only, zero pixel change, goldens re-verified in the same change — D24's sanctioned mechanical edit). Any testid **authored** in this phase must carry the `repositories-` prefix (files-and-naming R014); *ported* markup's testids are exempt under D23(e).

**8. Per-commit index self-consistency (toolchain R012). EVERY commit in this phase regenerates and entry-diffs — the per-step "`pnpm generate` last" reminders are emphasis, not an exhaustive list.** Steps 1, 4, 6 and 9 all add, move or delete files under `src/` or `tests/`, which both indexes walk (specs and goldens included), so each of them regenerates too. Phases 07 and 08 each lost a commit to exactly the inference that an unmentioned step does not need it. Run `pnpm generate` **per commit against that commit's own tree** (stash uncommitted files first) and diff `public/generated/{grep-index,fs-index}.json` **programmatically by entries** — both are single-line JSON, so `git diff` alone is useless, **and both are inherently unstable across runs** because they record the size of network-sourced `contributions.json` (00-phases.md, Deferred). Revert genuine live-data drift (`public/generated/contributions.json`, `src/generated/commits/daily-tech-digest.json`) with `git checkout --` before committing. **`src/generated/file-icons.json` is NOT live drift** — it is deterministic and tree-derived; commit it with the indexes. Expect substantial legitimate churn in it this phase as ~11 fixture files move into `src/`. **Regenerate last, after the final edit in each commit** — phase 07 lost a commit to regenerating before its last two comment fixes, and phase 08 repeated it one commit after its own plan warned about it.

**9. Long gates auto-background past the 120s foreground default — monitor to completion, never sleep-poll.** Three watchdog kills happened across phase 08; all landed on clean commit boundaries. If a gate is killed, re-run it from the committed tree rather than guessing at partial output, and never report a number you did not see.

## Steps

- [x] **1. Port the four e2e specs + three recipes' six goldens; add the project entries.**
  `git mv` `repositories.spec.ts`, `repositories-preview-highlight.spec.ts`, `repositories-preview-scroll.spec.ts` and `repositories-status-dots.spec.ts` into `src/features/repositories/tests/ui/e2e/`. Repoint each `fixtures` import to `../../../../../../common/tests/ui/support/fixtures` — **extensionless** (D23(d)) — after verifying the depth with `path.relative`. Fix `repositories.spec.ts`'s `ROOT` constant to the new depth (its `repos/` and `src/generated/commits/` targets do not move). Repoint the two `src/lib/highlight.ts` comment citations to `src/common/lib/highlight.ts` — **the file does not live there yet at this step; that is fine, step 3 creates it, and this is the only forward-looking repoint in the plan. Verify it exists before step 6's gate.**
  Create `src/features/repositories/tests/ui/visual/identical.spec.ts`, modeled on `src/features/help/tests/ui/visual/identical.spec.ts`'s shape (the two-source-array precedent), importing `recipes` **and** `extraRecipes` and filtering to a `REPOSITORIES_OWNED_RECIPE_NAMES` set of exactly `02-repositories`, `03-repositories-arrow`, `12-all-projects`. Keep the `api.github.com` abort `beforeEach`.
  `cp` all six goldens into `src/features/repositories/tests/ui/visual/goldens/<vp>/`; `cmp` each against the pre-move copy **and** against v1's copy at `/Users/shev/Development/spidey-hub/tests/visual/goldens/`; only then delete the originals. Add all three names to `tests/visual/identical.spec.ts`'s `SPLIT_OWNED_RECIPE_NAMES` (14 → 17) and update its header comment.
  Add the three project entries (§Mechanics 3); extend `package.json`'s `test:e2e` with the two `repositories-*` project flags and `test:visual` with the new `identical.spec.ts` path **only** (the harness spec path is added in step 8 and the adversarial paths in step 6 — Playwright exits 1 on a path that resolves to no tests). Sweep `docs/testing/{e2e,visual}/running-tests.md` in the same commit — match the existing rows' format exactly and **verify every project name against real `playwright test --list` output before writing any row**. This sweep is an edit *and* a deletion: both files name repositories in a "**Not yet real** for the remaining features (repositories, grep, shell-fs)" row, which must narrow to "(grep, shell-fs)" in the same change.
  *Verify:* `pnpm build`, then the full D20(a) project list **plus** `--project=repositories-1512x945 --project=repositories-1920x1080` → all green. Then `pnpm build:fixtures` and the `test:visual` list including the new `identical.spec.ts` → all green. `playwright test --list` names all three new projects. Capture the pre-move Shiki bundle baseline now: `grep -rlE "createHighlighterCore|shikijs" dist --include="*.js"` → record the result (expected: empty).

- [x] **2. R1 — extract `agoLabel` to `common/lib/ago-label.ts`. Its own commit, before anything else moves.**
  Create `src/common/lib/ago-label.ts` holding the function and its docstring **verbatim** (it has no imports). Delete it from `src/features/notifications/lib/notification-store.ts` — **no re-export shim**. Update the import lists in `src/features/notifications/components/notificationsState.svelte.ts`, `src/features/notifications/tests/unit/notification-store.test.ts` and `src/components/repositories/repositoriesState.svelte.ts` (the last still at its legacy path — repointing it now means step 5's move inherits a correct `common/` import, exactly as phase 08's fileIcons ordering did). Move the **two** `agoLabel` tests (`"agoLabel: seconds/minutes/hours/days thresholds"` and `"agoLabel: never negative even if ts is momentarily ahead of now"`) verbatim into a new `common/tests/unit/ago-label.test.ts`, importing from `../../../src/common/lib/ago-label`.
  `pnpm generate` last; diff both indexes by entries.
  *Verify:* `grep -rn "agoLabel" src common tests | grep -v "common/lib/ago-label\|common/tests/unit/ago-label"` shows only the three repointed import sites and their two call sites. `pnpm check` → 0 errors. `pnpm test:unit` → **346 passed / 21 files** (count unchanged, file count +1). Then prove the closed context did not regress: real build → `--project=notifications-1512x945 --project=notifications-1920x1080` green; fixture build → `--project=notifications-harness` green.

- [x] **3. R2 + R3 — promote `commits.ts`, `repoTree.ts` and `highlight.ts` to `common/lib/`. Its own commit, before the feature move.**
  **The order matters and is deliberate — do not swap it with step 5.** Promoting first means every consumer repoints while repositories is still at its legacy location, so when step 5 moves the feature, its own imports are already `../../common/...` imports covered by step 5's ordinary repoint instruction. Doing the move first would strand `repositoriesState.svelte.ts`'s `../../lib/repoTree` at a path that resolves to a nonexistent `src/features/lib/repoTree` — and nothing in step 5's verify could catch it, since `test ! -e` checks, greps and `pnpm test:unit` all pass over a broken Svelte import.
  `git mv src/lib/highlight.ts src/common/lib/highlight.ts` (no relative imports of its own — verify). `git mv src/lib/repoTree.ts src/common/lib/repo-tree.ts`; its `./highlight` import stays a sibling import, now `import type` from `./highlight` in the same directory. `git mv src/lib/commits.ts src/common/lib/commits.ts`; re-depth **both** globs — `../generated/commits/*.json` and `../../fixtures/commits/*.json` — **verify with `path.relative`, do not guess** (`src/generated/commits/` does not move; `fixtures/commits/` moves in step 5, so this glob is repointed **again** there — that is expected, not a mistake).
  `git mv tests/unit/repoTree.test.ts common/tests/unit/repo-tree.test.ts` and `tests/unit/highlight.test.ts common/tests/unit/highlight.test.ts`; repoint their imports to `../../../src/common/lib/{repo-tree,highlight}` and their header citations.
  Repoint every consumer, **path only**: six `src/pages/*.astro` (`getCommitsByRepo`), `src/bootstrap/Terminal.svelte`, `src/common/components/PaneTree.svelte` (`Commit`), `src/common/components/editor/{Editor.svelte,editor-render.ts,editorState.svelte.ts}` (`TokenSpan` → `../../lib/repo-tree`), `src/components/Shell.svelte`, `src/lib/shellIndex.ts` (**strip its explicit `.ts` extension while repointing** — toolchain R014, the line is being edited anyway), `src/lib/githubCommits.ts`, `src/components/repositories/repositoriesState.svelte.ts`, and `scripts/generate.mjs:51` (**keeps** its `.ts` extension — D23(d)). Repoint the stale comment citations in `src/lib/shell.ts`, `src/lib/shellIndex.ts`, `src/lib/githubTrees.ts`, `src/content.config.ts:7` and `generate.mjs`.
  `pnpm generate` last; diff both indexes by entries.
  *Verify:* `test ! -e src/lib/repoTree.ts && test ! -e src/lib/commits.ts && test ! -e src/lib/highlight.ts`; `grep -rn "lib/repoTree\|lib/commits\|lib/highlight" src common tests scripts` returns nothing. **`pnpm check` → 0 errors** — the gate that proves every repoint resolves. `pnpm test:unit` → 346/21. `pnpm build`, then `grep -rlE "createHighlighterCore|shikijs" dist --include="*.js"` → **must match step 1's baseline** (expected: empty). A non-empty result means the type-only boundary broke: **stop and report**, do not work around it.

- [x] **4. Move the feature's own source and libs; delete the legacy directory.**
  `git mv` all eight component files into `src/features/repositories/components/`, `src/lib/githubCommits.ts` → `src/features/repositories/lib/github-commits.ts`, `src/lib/githubTrees.ts` → `…/lib/github-trees.ts`. `git mv tests/unit/githubCommits.test.ts src/features/repositories/tests/unit/github-commits.test.ts` and `tests/unit/githubTrees.test.ts …/tests/unit/github-trees.test.ts`; repoint their imports (`../../lib/github-*`) and header citations.
  Repoint each component's own `../../common/...` imports to their new depths (`../../../common/...`, matching employment); `repositoriesState.svelte.ts`'s `github-commits`/`github-trees` imports to `../lib/…`; `src/common/components/PaneTree.svelte`'s `Repositories.svelte` import (R5, path only). Fix `Repositories.svelte:24`'s `src/lib/githubTrees.ts` comment citation.
  **Fix, in this same change, the two literals this move invalidates (D8) — neither may wait for step 5:** `views.test.ts`'s `expectedView()` **exact-match** line (`path === "src/components/repositories/Repositories.svelte"` → the new component path; its `src/content/repositories/` **prefix** line stays untouched here and is fixed in step 5), and **break #3** in `tests/e2e/grep.spec.ts` (the typed query, the `data-path` assertion and the line-137 comment). `Repositories.svelte` is in the real grep index — that is why the exact-match line exists — so regenerating without these two fixes goes red, and *not* regenerating commits an index stale against its own tree. Also repoint `generate.mjs`'s comment citation naming `src/components/repositories/repositoriesState.svelte.ts`.
  `pnpm generate` **last**; diff both indexes by entries.
  *Verify:* `test ! -d src/components/repositories`; `grep -rn "components/repositories\|lib/githubCommits\|lib/githubTrees" src common tests scripts docs | grep -v "^src/features/repositories"` returns only the intentionally-updated `PaneTree.svelte` line. **`pnpm check` → 0 errors.** `pnpm test:unit` → **346 passed / 21 files** — this is the gate that proves the `views.test.ts` exact-match repoint landed against the regenerated index, not a formality. Record the `PaneTree` edge in Results for the auditor, naming D23(c).

- [x] **5. Move the content tree, the yaml and all three fixture mechanisms; fix every out-of-context break; delete originals.** This is the phase's largest commit.
  `git mv src/content/repositories src/features/repositories/content/repositories` (**whole directory — R4's nested segment is load-bearing**), `src/data/repositories.yaml` → `src/features/repositories/content/repositories.yaml`, `fixtures/repositories` → `src/features/repositories/tests/ui/support/repositories`, `fixtures/repos` → `…/tests/ui/support/repos`, `fixtures/commits` → `…/tests/ui/support/commits`, `fixtures/contributions.json` → `…/tests/ui/support/contributions.json`. `git mv tests/unit/all-projects-fixture.test.ts src/features/repositories/tests/unit/all-projects-fixture.test.ts`.
  Fix, **all in this same change**: **break #1's remaining half** (`views.test.ts`'s `expectedView()` **content-prefix** literal — the exact-match component literal was already fixed in step 4); **break #2** (`docline.fixtures.json`'s **four** `"path"` values); **break #4** (`all-projects-fixture.test.ts`'s `ROOT`, two path constants and five message/comment strings); **break #5** (`generate.mjs`'s `srcDir`, its `console.warn` string and its remaining comment citations); **break #6** (`content.config.ts`'s ternary **both** branches + R8's header rewrite + the `generateId` comment; `common/lib/data.ts`'s `?raw` import and line-332 comment; `package.json`'s **two** `build:fixtures` `cp` sources; `common/lib/docline.ts:64`; `common/content/cmdline.yaml:116`). Re-depth `common/lib/commits.ts`'s `FIXTURE_GLOB` to the fixture commits' new home and update its `commitsDir` literals and header citations (R2).
  `pnpm generate` **last**; diff both indexes by entries — expect large, fully-accountable churn.
  *Verify:*
  ```bash
  test ! -d src/content/repositories && test ! -e src/data/repositories.yaml
  test ! -d fixtures/repositories && test ! -d fixtures/repos && test ! -d fixtures/commits && test ! -e fixtures/contributions.json
  ls fixtures   # must still list grep-index.json and fs-index.json, and nothing else
  ```
  Then both sweeps from §Background — every surviving hit must be an intentionally-updated line. **`pnpm check` → 0 errors.** `pnpm test:unit` → **346 passed / 21 files** (proves breaks #1, #2 and #4). Confirm `public/generated/grep-index.json` now carries `src/features/repositories/content/repositories/…` **and** that `grepPathToView()` still routes it — the `views.test.ts` pass is that proof (R4).

- [x] **6. R6 — split `adversarial-fixtures.spec.ts` and delete the root file.**
  Create `src/features/repositories/tests/ui/visual/adversarial-fixtures.spec.ts` with the repositories `describe` block and `src/features/employment/tests/ui/visual/adversarial-fixtures.spec.ts` with the employment one, each carrying its own verbatim copy of `gotoReady`, `prefixDigit` and `fitsWithin` and its own re-depthed, **extensionless** `BOOT_SEEN_STORAGE_KEY` import. Rewrite each file's header in its own terms — describing the fixtures it actually exercises, at their **new** paths, with no "Phase 7b.2" citation (comments.md R008). Test bodies, locators and assertions are **untouchable** (D23(a)). Delete `tests/visual/adversarial-fixtures.spec.ts`. In `package.json`'s `test:visual`, replace `tests/visual/adversarial-fixtures.spec.ts` with the two new paths.
  *Verify:* `test ! -e tests/visual/adversarial-fixtures.spec.ts`; `ls tests/visual` shows only `identical.spec.ts`, `goldens/` and `README-PIPELINE.md`. Fixture build → `pnpm test:visual` green as a whole, with the adversarial test count still **8** (2 tests × 2 viewports × 2 files), not 4 and not 16.

- [ ] **7. Full gate, from the clean committed tree.** Not folded into any prior commit.
  *Verify:* `pnpm check` → 0 errors. `pnpm lint` → exit 0. `pnpm test:unit` → 346/21. D20(a) real build → `smoke`, `legacy-*`, `common-*`, `profile-*`, `help-*`, `boot-*`, `notifications-*`, `dashboard-*`, `employment-*`, `repositories-*` all green. D20(b) fixture build → every `*-visual` + legacy visual + both adversarial halves + the **six existing** harness specs green (repositories' own harness lands in step 9 and is gated there). `grep -rlE "createHighlighterCore|shikijs" dist --include="*.js"` still matches step 1's baseline. `public/generated/*` unchanged by this step. `node scripts/generate.mjs` completes with all five artifact kinds and **throws nothing** — its keep-existing fallbacks make a missing network acceptable, but a `[generate] … not found — skipping` warning naming a repositories path is a **failure**, not a fallback (break #5).
  **Watch the carried-in flake here.** If `repositories.spec.ts` fails, rebuild from clean and re-run before concluding anything; if it reproduces, root-cause it in this phase rather than re-deferring it.

- [ ] **8. Golden parity — zero churn.**
  *Verify:* `diff -rq tests/visual/goldens /Users/shev/Development/spidey-hub/tests/visual/goldens` shows **only** `Only in …` lines for every recipe split out by phases 02–09 — never a `differ` line. `cmp` each of this phase's six goldens against v1's byte-for-byte. Repo-wide total stays 21 recipes × 2 viewports = 42 PNGs: root **8** (4 recipes: `09-grep-empty`, `10-grep-query`, `16-shell`, `17-host-shell`), common 10, repositories 6, other features 18. **No `--update-snapshots`, ever (D5).**

- [ ] **9. Feature harness (D24).**
  Add the `repositories` branch to `src/pages/harness/[feature].astro` (§Mechanics 4), build `RepositoriesHarness.svelte` with the keydown→`handleKey()` routing and its documenting header comment (§Mechanics 5), the ready-element, a `RepositoriesPage` page object (§Mechanics 6 — this is what closes `TerminalPage.ts:7`'s forward reference), and specs at `src/features/repositories/tests/ui/harness/repositories.spec.ts` asserting mount plus the feature's core interactions: the repos panel lists the fixture repos, selecting a repo populates the files tree, selecting a file renders preview lines, the commits panel renders the fixture commit snapshot, and the status panel renders its contribution grid from the fixture `contributions.json`. (Whether the fixture `repos/*.json` carry real token spans is not asserted in this inventory — derive what to assert from the actual fixture data rather than assuming highlighted tokens are present.) Abort `api.github.com` in a `beforeEach`. Assert kernel-free isolation via the composed `StatusBarPage`: `expect(page.statusBar.windows).toHaveCount(0)`. No goldens (D24(b)).
  Now that the spec exists, append its path to `package.json`'s `test:visual`.
  *Verify:* fixture build → `--project=repositories-harness` green, and `pnpm test:visual` green as a whole with the new path in it. Real build → `test ! -d dist/harness` **and** `grep -rl "repositories-harness-ready" dist --include="*.html"` empty. Use the harness-only ready-marker for that grep, **never** a bare component name — real pages legitimately contain repositories markup. A bare `find dist -iname "*harness*"` will still match dead ~1 KB JS chunks from earlier phases — expected and pre-ruled harmless.

- [ ] **10. Record and close.**
  Run **all three** citation sweeps over this phase's own context — the numbered form, the bare-noun form, and the path-existence form that 00-phases.md makes mandatory for phases 09–11:
  ```bash
  grep -rniE "plan\.md|phase [0-9]|defect [0-9]|decision [0-9]|iteration [0-9]|\b[fg][0-9]+\b" src/features/repositories
  grep -rniE "\b(the )?(audit|cleanup|criterion|rebuild|rewrite|refactor|mockup|prototype|spec) ('s)?\b" src/features/repositories
  grep -rnoE "(src|tests|common|Instructions|docs)/[A-Za-z0-9_./-]+|[A-Za-z0-9_-]+\.(md|dc\.html|mjs|ts)" src/features/repositories
  ```
  The third's output must be checked **path by path against the real filesystem** — every path-shaped string it returns has to resolve to a file that exists. This subsumes comments.md R003 as well as R008 and is the only form that catches a citation of a v1 document by name (`UI-Mockups/…`, `*.dc.html`, `Homepage.dc.html`, `docs/changes/…`). Also run the first two over `src/common/lib/{commits,repo-tree,highlight,ago-label}.ts` and `common/tests/unit/{repo-tree,highlight,ago-label}.test.ts`, which this phase authored into common. The second grep is deliberately over-broad and returns mostly legitimate prose — read every hit. Judgment rule: a reference to a **process artifact that does not exist in this repository** (a v1 audit, a v1 plan, a numbered decision log, a superseded cleanup pass) is the violation; a reference naming a **current constraint the code implements** — "Homepage.dc.html's sample commits arrays" as the stated provenance of a fixture that still exists — is not, and must be left alone. **Comment text only; a ported spec's logic and assertions are untouchable.**
  Then fill in Results: every file moved, every importer updated, every out-of-context break and how each was verified fixed, the D23 edges flagged for the auditor, gate outputs with real numbers, the harness shape used, the final golden distribution, and the R6 helper duplication. Commit to `frontend-rewrite` — single-line imperative, **no body, no trailer** (toolchain R011), one reviewable unit per commit (R012).

## Acceptance criteria

1. Every file in the move table is at its new path; `src/components/repositories/`, `src/content/repositories/`, `src/data/repositories.yaml`, `src/lib/{commits,repoTree,highlight,githubCommits,githubTrees}.ts`, `fixtures/{repositories,repos,commits}/` and `fixtures/contributions.json` no longer exist. `fixtures/` still holds `grep-index.json` and `fs-index.json`, and nothing else.
2. The content tree sits at `src/features/repositories/content/repositories/` with its `content/repositories/` segment pair intact, and `views.test.ts` passes against the regenerated real index (R4).
3. All six out-of-context break classes are fixed and proven by a green `pnpm test:unit` (346/21) and a green real-build `legacy-*` run.
4. `agoLabel` lives in `common/lib/`, no feature imports another feature, and notifications' own e2e + harness + unit gates are green (R1).
5. `commits.ts`, `repo-tree.ts` and `highlight.ts` are in `common/lib/` with every consumer repointed (R2, R3); `pnpm check` proves resolution.
6. The generate-time-only Shiki boundary holds: `grep -rlE "createHighlighterCore|shikijs" dist --include="*.js"` matches the pre-move baseline on every build.
7. `content.config.ts`'s ternary updated on **both** branches, its stale header prose fixed (R8), and `build:fixtures`'s **two** `cp` sources repointed with destinations unchanged (R7).
8. `pnpm check` 0 errors, `pnpm lint` 0 findings, `pnpm test:unit` 346 tests / 21 files.
9. D20 both invocations green, including `repositories-{1512x945,1920x1080}`, `repositories-visual-*`, `repositories-harness`, and both adversarial halves (8 tests total, R6).
10. `node scripts/generate.mjs` completes with all five artifact kinds and emits no "not found — skipping" warning naming a repositories path.
11. Zero golden churn; 42 PNGs repo-wide (root 8 / common 10 / repositories 6 / others 18); all three repositories recipes byte-identical to v1 at both viewports.
12. Real build emits no `dist/harness/` directory and no HTML page referencing the repositories harness marker; `RepositoriesPage.ts` exists, closing `TerminalPage.ts:7`'s forward reference.
13. All three citation sweeps clean over `src/features/repositories` and over the four common modules this phase authored there; every path-shaped string resolves on disk.
14. Verifier PASS **and** auditor clean on `src/features/repositories` (D23 exemptions recorded with the exemption named).
15. Results filled in; no `(executor fills in)` placeholder.

## Stop conditions

- **No behavior changes.** No repo-browse, preview, commits, status-dot or keyboard-model edits. If a spec goes red on *logic*, the move is wrong, not the spec. **This does not forbid D8 path updates** — repointing an import or a literal filesystem path a move invalidated is required. The line is behavior vs. location.
- **No generator redesign.** `generate.mjs` changes are the one `srcDir` constant, its warn string, its `highlight.ts` import path and its comment citations — nothing else. `OWNER`, `REPOS`, `GREP_ROOT_SUBDIRS`, `FS_INDEX_SUBDIRS` and every `outDir` stay as they are.
- **No icon or theme changes**; `material-file-icons` and Shiki stay build-time-only.
- **No flattening the content tree.** The `content/repositories/` segment pair is load-bearing through `grepPathToView()` (R4). Move the whole parent directory; never rename a leaf. The `.md` filenames' case is load-bearing through `generateId` — never rename one.
- **No rewriting moved code** — no splitting `repositoriesState.svelte.ts` (R10: it is D23(e)-exempt; leave its `private` fields, field ordering, `handleKey` signature and module-scope pure helpers alone), no normalizing any ported spec, no extracting the R6 helper duplication.
- **No inventing `cp` clauses.** Only the two existing ones change source paths (R7).
- **No golden regeneration**, no `--update-snapshots`.
- **No behavioral edits outside the context** — path updates, the R1 extraction (behavior-free) and the R6 split only.
- **Do not touch** `repos/`, `src/generated/`, `public/generated/`, `tests/unit/keyframes-dedup.test.ts`, `src/common/lib/views.ts`, or `vitest.config.ts`. If you believe one needs editing, **stop and report**.
- If a step turns out to rest on a false assumption, **stop and report** rather than redesigning; the orchestrator updates this plan.

## Results

(executor fills in)

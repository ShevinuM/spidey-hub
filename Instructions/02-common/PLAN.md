# Phase 02 — common

> **Status: planned, not started. Blocked on phase 01.** Binding decisions: `Instructions/00-phases.md` (D5–D10, D15–D17, D20–D23).

## Context

`src/common` — engines, shared components, shared lib, and `common/tests/`. **Declared out-of-context wiring:** Terminal.svelte/terminalState + layout → `src/bootstrap/` (composition root, D10); thinning `src/pages/*.astro` to import only from bootstrap; one `common` Playwright project entry; mechanical import-path updates across the legacy tree and specs (legacy-fallout rule, 00-phases.md). Because of the bootstrap wiring, this phase's gate is the full suite, not a scoped run.

## Objective

Everything shared — tmux/vim engines, editor, kernel chrome, shared lib — lives under `src/common/` per architecture R001/R006/R007; the composition root lives in `src/bootstrap/`; the 12 kernel+editor specs run green as the `common` project; goldens unchanged.

## Background — move table (v1 names → v2 homes; renames per D15)

| v1 | v2 |
|---|---|
| `src/lib/tmux.ts` | `src/common/engines/tmux/tmux.ts` (feature-agnostic — R007: no feature-name unions; feature knowledge injected from bootstrap) |
| `src/lib/vim.ts` | `src/common/engines/vim/vim.ts` |
| `src/components/editor/*` (`Editor.svelte`, `EditorBuffer.svelte`, `EditorStatusLine.svelte`, `editorState.svelte.ts`) | `src/common/components/editor/` |
| `src/lib/editorRender.ts` | `src/common/components/editor/editor-render.ts` (or `common/lib/` if grep shows consumers beyond editor) |
| `src/components/{StatusBar,Meter,PaneTree,PanelBadge,Wallpaper,Cmdline,CopyMode,ChooseTree}.svelte` | `src/common/components/` |
| `src/lib/{clock,views,layout,pasteTargets,pasteBuffer,cmdline,data}.ts` | `src/common/lib/` (kebab-case: `paste-targets.ts`, `paste-buffer.ts`) |
| `src/components/terminal/{Terminal.svelte,terminalState.svelte.ts}` | `src/bootstrap/` |
| `src/layouts/Shell.astro` | `src/bootstrap/Layout.astro` (D13 — resolves the `Shell.svelte` collision; rendering-neutral) |
| `src/data/{site,choosetree,cmdline}.yaml` | `src/common/content/` with `data.ts` glob widened per D17 |
| specs: `terminal, tmux, panes, sessions, choose-tree, copy-mode, cmdline, nav, panel-badge, animations, editor-vim, editor-cursor-visibility` | `common/tests/ui/e2e/` |
| unit: `tmux, cmdline, views, vim` `.test.ts` | `common/tests/unit/` (or engine-adjacent per unit-testing.md R001 — settle once, record here) |

Judgment calls for the executor (consult advisor): `pages/*.astro` and `src/content.config.ts` stay put (D10 — Astro hard-codes them). `cmdline.spec.ts` imports `src/lib/grep.ts` — grep.ts does **not** move this phase; only its import path in the spec survives unchanged. Any module in the table whose grep shows a single feature consumer stays put for that feature's phase instead (D16) — record deviations in Results.

## Steps

- [x] **1. Port specs.** Move the 12 specs verbatim (import paths only) → `common/tests/ui/e2e/`; add `common` project entry; shrink `legacy` scope accordingly. Seed common-mapped goldens (per `Instructions/01-pre-phase/recipe-feature-map.md`) → `common/tests/ui/visual/goldens/<viewport>/` byte-identical (`cp` + `cmp`), with a `common-visual` project reusing the D21 mechanics **only if** phase 03 has recorded them; if 02 runs before 03, this phase settles D21 itself and records it in the map file.
  *Verify (D20):* real build → `--project=common --project=legacy --project=smoke` green against un-moved source; fixture build → visual green; goldens `cmp`-identical.
  *Result:* done. Phase 02 ran first, so it settled D21 itself (full record in `recipe-feature-map.md`'s "D21 snapshot-path decision" section). 12 specs moved with import-path/extension fixes; shared visual infra (`pipeline.mjs`, `recipes.ts`, `static-server.mjs`, `capture-goldens.mjs`, `checkFixtureFlag.ts`→`check-fixture-flag.ts`, `reference/`) moved to `common/tests/ui/support/` (top-level, matching pre-phase's own `fixtures.ts`/`content-fixtures.ts` precedent there, not `src/common/tests/`). Added `common-<viewport>` e2e projects and `common-visual-<viewport>` visual projects (each with its own hardcoded-viewport `snapshotPathTemplate`, D21(a)) to `playwright.config.ts`; updated `package.json` (`goldens`, `test:visual`, `test:e2e`), `tsconfig.json` exclude, `.oxlintrc.json` ignore pattern, and 2 build-tooling scripts (`scripts/capture-screenshots.mjs`, `scripts/generate-design-mirror.mjs`) that imported the moved infra directly. Seeded 8 PNGs (4 recipes × 2 viewports: `06-editor`, `15-cmdline`, `18-split`, `19-choose-tree`) to `common/tests/ui/visual/goldens/`, `cmp`-verified byte-identical against v1, then removed the now-orphaned originals from `tests/visual/goldens/`. Gates run against un-moved source (source moves in step 2): real build → `smoke`+`legacy-1512x945`+`legacy-1920x1080`+`common-1512x945`+`common-1920x1080` = 1012 passed; fixture build → `tests/visual/identical.spec.ts` (17 recipes, common's 4 filtered out via `COMMON_OWNED_RECIPE_NAMES`) + `adversarial-fixtures.spec.ts` + new `common/tests/ui/visual/identical.spec.ts` (the 4 common recipes) = 50 passed. `pnpm check` and `pnpm lint` both clean (exit 0).
- [x] **2. Move source** per the table; update every import across legacy tree, specs, `common/tests/ui/support/` fixtures; literal-path assertions per D8; delete originals.
  *Verify:* old paths absent; `grep -rn "src/lib/tmux\|src/lib/vim\|components/editor\|layouts/Shell" src tests` empty (excluding goldens/generated).
  *Result:* done, in 3 gated sub-moves (each its own commit, each verified green on `pnpm check`+build+full D20 e2e gate+fixture visual gate before proceeding):
  (a) `tmux.ts`→`common/engines/tmux/tmux.ts`, `vim.ts`→`common/engines/vim/vim.ts`, `{clock,views,layout,cmdline,data}.ts`+`pasteTargets.ts`→`paste-targets.ts`+`pasteBuffer.ts`→`paste-buffer.ts`→`common/lib/`, `{site,choosetree,cmdline}.yaml`→`src/common/content/` with `data.ts`'s per-file `?raw` imports repointed (D17 — see note below);
  (b) editor folder + `editorRender.ts`→`editor-render.ts` co-located → `common/components/editor/`, the 8 chrome components → `common/components/`;
  (c) `Terminal.svelte`+`terminalState.svelte.ts`+`Shell.astro`→`Layout.astro` → `src/bootstrap/`, 6 pages' `Shell`/`Terminal` imports repointed and the `<Shell>`→`<Layout>` tag rename applied.
  Old-path verify grep is clean (`src/lib/tmux`, `src/lib/vim`, `components/editor` outside `common/`, `layouts/Shell` all absent from `src`/`tests`/`common`, excluding the vendored `common/tests/ui/support/reference/` prototype and two pre-existing synthetic test fixtures — `tests/unit/grep.test.ts`/`shell.test.ts` — that use old-looking paths as frozen, hand-built sample data unrelated to the real tree).

  **Judgment calls (consulted advisor before each):**
  - **D21(a)+(b) settled by this phase** — see `recipe-feature-map.md`'s "D21 snapshot-path decision" section (step 1's Result already covers this in detail).
  - **tmux.ts R007 fix (architecturally the largest call this phase):** `ProgramName` was a hardcoded 7-value feature-name union (`"dashboard"|"repositories"|...|"shell"`), a genuine R007 violation. Widened to `export type ProgramName = string` (kept the exported alias so every one of its 6 in-file usages and every external importer keeps compiling unchanged — type erasure means zero runtime delta, confirmed no consumer relies on it for exhaustiveness via a full `grep -rn "ProgramName"` sweep first). Dropped the now-redundant `seed.id as ProgramName` cast; fixed the stale "site.yaml's six window ids" comment. The `program === "shell"` branch in `programDisplayName`/`exitProgram` was kept as-is — ruled engine-intrinsic (real tmux's own "a pane's default program is a shell" semantics), not feature knowledge, since threading a `defaultProgram` sentinel through `FactoryOptions`/`Session`/`Client` would be genuine engine redesign (the stop condition), not mechanical parameter-threading. Flagged for the phase-close auditor: D23 carries no blanket exemption for R007, so if the auditor reads this branch as a "no," that's an orchestrator call, not something preempted here.
  - **PaneTree.svelte → common, importing 6 still-transient feature components (Dashboard/Repositories/EmploymentRecords/Profile/HelpView/Shell) unchanged:** ruled NOT a violation to fix — D10's own move table places PaneTree in this phase's move, the imported components all still live in the pre-bounded-context transient legacy tree (D1: "structure audits are phase-scoped until then"), and D23(c) exempts exactly this class of transient-tree hit from blocking phase close. Recorded here rather than redesigned into an injected registry (which would trip "move, don't rewrite" AND preempt the R008 composition mechanism phase 03+ establishes once features actually exist). Swept the other 7 chrome + 4 editor files for the same pattern — PaneTree is the only one.
  - **Terminal.svelte → bootstrap, importing Shell/Notifications/GrepOverlay/BootSequence/HelpSearch:** NOT the PaneTree case — for the composition root, importing every feature (even transient ones) is R008's literal definition, not a tolerated edge. No concern.
  - **Pages "import only from bootstrap" (D10) — interpreted narrowly:** the 6 pages continue to import `getSite`/`getDashboard`/etc. directly from `common/lib/data` (already fixed to the new path in sub-move (a)) rather than being routed through some new bootstrap re-export surface. Rationale: (1) the move table sends exactly 3 things to bootstrap this phase — Terminal.svelte, terminalState.svelte.ts, Shell.astro→Layout.astro — nothing designs a bootstrap data-assembly module; (2) D15 bans barrel `index.ts` files, which a re-export shim would effectively be; (3) R004 lets every layer depend on `common/`, and pages threading plain data as props is exactly the props-down shape R008 wants — the actual composition (instantiating/wiring features) lives entirely in Terminal.svelte, which is what's becoming bootstrap. Only the two literal composition pieces (`Shell`→`Layout`, `Terminal`) were rewired to import from `src/bootstrap/`. `../lib/commits`'s `getCommitsByRepo` import also stays as a transient-legacy edge (D1/D23(c); closes when the repositories phase claims `commits.ts`). If the phase-close auditor reads D10's "only from bootstrap" literally, that's an orchestrator ruling to make then, not preempted here with an invented API.
  - **`shell.ts` NOT promoted to `common/lib/`,** despite now having 3 real consumers (tmux engine, bootstrap's terminalState, and the still-transient Shell.svelte feature component) — D16's rule-of-three governs promotion of modules already named in the move table; `shell.ts` isn't in it, and promoting an un-listed module is scope creep beyond "src/common + declared wiring." Flagged here as a promotion candidate for whichever phase (11, shell-fs) next has reason to touch it.
  - **D17 "data.ts's glob" — clarifying finding, not a deviation:** `data.ts` has no `import.meta.glob` at all; it's 13 explicit per-file `?raw` imports (Vite-inlined at build time). "Widened to span `src/data/` + `src/common/content/`" was achieved the only way this file's shape allows: each of the 3 moved yaml files' own `?raw` import was repointed to `../content/<name>.yaml?raw`, the other 10 stayed pointed at `../../data/<name>.yaml?raw`. Functionally identical outcome to a widened glob; no glob mechanism was introduced (would have been unrequested rework of a file that isn't broken).
  - **Grep-index content-collision fallout (D8, found via the full e2e gate, not inspection):** `tests/e2e/grep.spec.ts`'s "Enter on a Repositories.svelte hit lands in the repositories view" test typed the query `"components/repositories/Repositories.svelte"`, previously guaranteed to match only the real file's own PATH. Once `PaneTree.svelte` moved to `common/components/` its own relative import literal (`../../components/repositories/Repositories.svelte`) started containing that exact fragment as file CONTENT, and because `src/common/` sorts alphabetically before `src/components/` in the generated grep index, PaneTree's content hit started winning the "first result" race the test asserts on (`search()` in `src/lib/grep.ts` has no relevance scoring — it's pure file-array order, path-hit-then-content-hit per file). Fixed by prefixing the typed query with `"src/"` (a relative import literal never spells that out, so the fragment stays unique to the real file's own path) — a test-only change, D8-sanctioned ("literal-path assertions... updated in the same change as the move that invalidates them"), verified by re-running the isolated test before and after.
  - Both `advisor()` calls before this step (source-move judgment calls) and the standing PLAN.md contract were followed throughout; no deviation from either.
- [ ] **3. Full gate.** `pnpm check` + lint + `pnpm test:unit` + D20 both invocations (all projects).
- [ ] **4. Golden parity.** Zero golden churn anywhere (`diff -rq` vs v1 slices).
- [ ] **5. Move unit tests** per table; Vitest globs widened once to cover `common/tests/unit` + `src/features/*/tests/unit`.
  *Verify:* `pnpm test:unit` green, same test count as phase 01 close.

## Acceptance criteria

Full D20 gate green; check/lint/unit green; goldens byte-identical; architecture R004 holds for everything moved (common imports no feature; engines feature-agnostic per R007); verifier PASS + auditor clean on `src/common` (D23 exemptions apply); commit per 00-phases.md discipline (contingent on git question). Stop conditions: no engine redesign (move, don't rewrite); no POM/locator refactors; no golden regeneration; nothing moved into `features/`.

## Results

(executor fills in)

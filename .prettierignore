# Generated artifacts — scripts/generate.mjs owns these bytes.
public/generated/**
src/generated/**

# Gitleaks SARIF report — the gate's secret-scan step writes this into the
# workspace root before pnpm format runs, so it only ever exists in CI.
results.sarif

# Frozen fixture bytes — a reformat here changes what a test compares against.
**/goldens/**

# Copied verbatim into dist/generated/ by build:fixtures — these bytes reach the rendered artifact.
src/features/grep/tests/ui/support/grep-index.json
src/features/repositories/tests/ui/support/repos/*.json
src/features/shell-fs/tests/ui/support/fs-index.json
src/features/repositories/tests/ui/support/contributions.json

# Rendered one source line per terminal row, and prettier inserts a blank line after YAML
# frontmatter and after every ATX heading, so a reformat here is a pixel change by construction.
src/features/employment/content/personnel/**/*.md
src/features/employment/tests/ui/support/personnel/**/*.md
src/features/repositories/content/repositories/*.md
src/features/repositories/tests/ui/support/repositories/*.md

# Vendored third-party design handoff — not this repo's source.
reference/

# Build outputs and caches.
.cache/
dist/
coverage/

# Machine-managed lockfile.
pnpm-lock.yaml

# Agent tooling state, not project source — untracked and outside the build.
.claude/
.remember/
Tasks/

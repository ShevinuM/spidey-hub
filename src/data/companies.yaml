# Root-directory display order for the Personnel Files tree
# (Personnel.svelte). `name` must match the top-level directory name under
# src/content/personnel/ exactly. PLAN.md Iteration 3 Phase 1 item 1.3: the
# tree below root is now derived purely from content file paths (no more
# per-role `company` frontmatter matching) — this file's only remaining job
# is ordering the two root entries.
- name: enaimco
  order: 0
- name: memorial-university
  order: 1

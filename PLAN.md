# PLAN — Iteration 6: SpideyHub (UI v2, renames, refactor, docs)

Source of intent: `prompt.md` (user's notes — DO NOT edit/delete until Phase J).
Mockups: `UI-Mockups/builds-page-design-review/` (Builds/Dashboard/Help/Personnel `.dc.html` + `*-Changes.md`).
Sub-prompts: `refactor-components-prompt.md`, `notifications-prompt.md` (stale in part — see D6).

## Objective

Ship UI v2 across Builds→Repositories, Employment Records, Help, Dashboard, and notifications per the mockups; refactor the five monolith components into the folder+state-class pattern behind a test safety net; unify naming (repositories / Employment Records / repos window); write docs, README, and agent files; then gitignore mockups, delete prompt/PLAN files, and rename the folder to SpideyHub.

## Context: verified facts (from 4 research passes)

- Stack: Astro + Svelte 5 runes, static output. Tests: `pnpm check`, `pnpm test:unit` (node --test), `pnpm test:e2e` (Playwright, 421 tests), `pnpm test:visual` (fixture build `PORTFOLIO_FIXTURES=1`, 20 recipes × 2 viewports, goldens at `tests/visual/goldens/`). Rebaseline: `pnpm build:fixtures && pnpm exec playwright test tests/visual/identical.spec.ts --update-snapshots`.
- Coverage is strong for Builds(31)/Editor(24)/Personnel(59)/Notifications(20)/Help(22); **Terminal and Dashboard have no dedicated spec** (incidental coverage only).
- Data convention (actual): `src/data/*.yaml` = hand-authored UI copy; `src/content/**/*.md` = hand-authored content (Zod-validated); `public/generated/` + `src/generated/` = machine-generated JSON from `scripts/generate.mjs` (predev/prebuild). **Decision: keep both folders** — prompt says keep if there's a reason; there is. Document the convention in docs/architecture.md.
- Notifications spec (toasts/sense-ring/header-sweep) is **already implemented exactly** (commits 2ef0d8c, a5d8393); `notifications-prompt.md` predates them. Remaining: pool-exhaustion re-circulation + a golden for the open panel. fixtureMode does NOT leak into dev/prod (env only set by build:fixtures script). Sense-ring unread-only gating is the spec itself — keep.
- Current Builds.svelte panel numbering is the OLD scheme (0=Changes,1=Status,2=Files,3=Repos,4=Commits, log unnumbered). Mockup = reading order 0..5 with Command Log numbered/focusable.
- Personnel.svelte is still the OLD drill-down browser (tree, `../`, `f` filter, bottom prompt + hint bars). Mockup = flat 6-row list + middle timeline column + index block. Current Personnel has exactly ONE animation: filter-cursor `blk` blink (removed with the filter bar).
- Help ALL-CAPS is 100% CSS/markup (4× `text-transform:uppercase` at HelpView.svelte:126/190/222/242 + literal `SCOPES` at :172). Mockup has ZERO text-transform; copy is already Title Case. Removing all five = user instruction AND mockup parity.
- The pill badge is byte-identical across Builds/Personnel mockups (blue-border red-glow variant) and Help uses a teal variant → build ONE shared `PanelBadge.svelte`.
- Assets missing from repo (copy from `UI-Mockups/builds-page-design-review/assets/` → `public/assets/`): `spider-glyph-red.svg`, `spider-web-red.svg`, `spiderman-teal.svg` (+ `spider-glyph-{blue,teal,gold}.svg` ONLY if Personnel corner sigils turn out enabled in the mockup's data-props — see E).
- GitHub contribution data: no unauthenticated API exists. Plan: build-time step in `scripts/generate.mjs` — GraphQL `contributionsCollection.contributionCalendar` when `GITHUB_TOKEN` is set; else scrape the public `github.com/users/<owner>/contributions` HTML (`td.ContributionCalendar-day` with `data-level`/`data-date`); else keep prior snapshot. Owner = same constant the commit-snapshot step already uses. Output `public/generated/contributions.json` (committed), plus `fixtures/contributions.json` (deterministic) used by build:fixtures.
- Operational: executors MUST redirect long build/test output to files under the scratchpad or /tmp (`... > /tmp/out.log 2>&1; tail -40 /tmp/out.log`) — long streams stall subagents. Commits: single line, no body, no Co-Authored-By. NEVER touch `prompt.md`/`notifications-prompt.md`/`refactor-components-prompt.md`/`UI-Mockups/` until Phase J.

## Decisions made without the user (review in the morning)

1. Keep `src/data` + generated folders (reason exists; documented instead of restructured).
2. Contribution chart: build-time generated ("live at build" like the commits pane), GraphQL→HTML-scrape→stale-snapshot fallback chain; mockup's red 5-level palette kept verbatim.
3. Commits-pane graph SVG: rendered from REAL visible commit rows (simple vertical spine + node per row; no fake merge topology copied from the mockup).
4. Status pane copy: mockup literal (`shevinum.dev → N repos`, computed count; drop "projects tracked").
5. Command log: single `src/content/command-log/command-log.md`, markdown body lines; `**bold**` → emphasize, `[text](href)` → links, mapped at build to existing `CommandLogLine` type.
6. Notification re-circulation: when `pickRandomUnseen()` is empty, `injectVisit()` re-injects the single OLDEST archived non-spam entry (back to inbox, unread, fresh timestamp, spawns toast). Unit-tested.
7. Personnel `f` filter + drill-down + `../` are dropped (mockup/prompt removed the bar); j/k/enter selection stays, preview follows selection.
8. Full rename builds→repositories: ViewId, route `/repositories`, `ProgramName`, component folder, `builds.yaml`→`repositories.yaml`, testids `builds-*`→`repositories-*`, e2e spec filename, site.yaml window 1 `{id: repositories, name: repos}` → status bar `1:repos`. Virtual repo stays literally `all-projects` (mockup shows that string).
9. Employment rename is sitewide incl. window 2 `{id: employment, name: employment}`, help scope file, cmdline copy, notification md copy, README.
10. Corner sigils: include ONLY if the Personnel mockup's data-props actually enables them; else skip (mock-editor tooling).
11. Existing `.claude/checklist.md` folds into new root `AGENT_CHECKLIST.md`.
12. B3 (Personnel relocation refactor) is SKIPPED per advisor review: Phase E deletes the drill-down state machine it would relocate. Phase E builds the rebuilt page directly in the target folder+state-class pattern, satisfying the refactor prompt's goal without the throwaway relocation.

**Verifier note — known dirt to IGNORE:** untracked `prompt.md`, `notifications-prompt.md`, `refactor-components-prompt.md`, `UI-Mockups/`, `.memsearch/` are the user's/session's files and stay untracked until Phase J. A housekeeping commit lands the pre-existing regenerated JSON changes + this PLAN.md before Phase A.

## Steps

Phases run in order; each phase = fresh executor, then verifier, then commit(s) already made by executor are checked. Golden rebaselines happen ONLY where marked ⟲, deliberately, with before/after review.

### Phase A — Test safety net (pre-refactor)
- [x] A1. Add `tests/e2e/terminal.spec.ts`: keydown dispatch order (prefix `C-b` mode entry/exit, digit window jump, key reaching the active view not others), view routing on navigation, reboot `r`, status-bar active-window highlight. ~10-14 tests, follow style of `tests/e2e/nav.spec.ts` (use its helpers; avoid the known clock.install+pauseAt race — use setFixedTime pattern from b43d519).
- [x] A2. Add `tests/e2e/dashboard.spec.ts`: menu labels + key hints from `src/data/dashboard.yaml`, menu click/keyboard navigation to each view, synced-panes line, wordmark presence. ~6-10 tests.
- [x] A3. Commit each spec separately (e.g. `Add terminal e2e spec`, `Add dashboard e2e spec`).
- Verify: `pnpm check` clean; `pnpm test:e2e` green incl. new specs (run new specs first in isolation: `pnpm exec playwright test tests/e2e/terminal.spec.ts tests/e2e/dashboard.spec.ts`); new tests assert real behavior (verifier spot-reads them); no source files changed except tests.

### Phase B — Component refactor (pure relocation; goldens are the gate, NEVER rebaseline here)
Follow `refactor-components-prompt.md` EXACTLY (it is the contract: target pattern, hard rules 1-6, per-component order Builds→Editor→Personnel→Notifications→Terminal, then the audit of the remaining 14 components).
- [x] B1. Builds → `src/components/builds/` per the file plan in that prompt + `src/lib/commandLog.ts`. Commit.
- [x] B2. Editor → `src/components/editor/`. Commit.
- [ ] B3. SKIPPED per Decision 12 — Personnel is rebuilt from scratch in Phase E directly in the target pattern.
- [x] B4. Notifications → `src/components/notifications/`. Commit.
- [ ] B5. Terminal → partial split allowed per the prompt's Terminal note. Commit.
- [ ] B6. Audit remaining 14 components per criteria (a)-(d); apply (c)-only moves cheaply; full pattern only if 2+ of a/b/d; write one-line dispositions into the executor report. Commit(s).
- Verify after EACH component (executor runs, verifier re-runs at phase end): `pnpm check && pnpm test:unit && pnpm test:visual && pnpm test:e2e` all green, goldens byte-unchanged (`git status tests/visual/goldens` clean), every `bind:this` export still resolved (grep callers), no testid/DOM/comment changes (`git diff` sampled by verifier for moved-not-changed).

### Phase C — Naming sweep 1: collection + Employment Records ⟲
- [ ] C1. `src/content/projects` → `src/content/repositories`; `fixtures/projects` → `fixtures/repositories`; update `src/content.config.ts` (collection name + fixture switch), all `getCollection("projects")` call sites (6 pages), `src/lib/views.ts` menu-id mapping + `grepPathToView()`, `src/lib/commits.ts`, `tests/unit/all-projects-fixture.test.ts`, `scripts/generate.mjs` references. Generated artifact `public/generated/repos/all-projects.json` name and the virtual repo name `all-projects` stay. **Fixture index copies too**: `build:fixtures` overwrites `grep-index.json`/`fs-index.json`/`repos/all-projects.json` from `fixtures/` copies which embed `src/content/projects/...` paths — determine how those fixture JSONs are produced (script vs hand-maintained) and update them for the renamed paths, else goldens/fixture e2e keep rendering old paths.
- [ ] C2. Personnel Files → Employment Records sitewide: `src/data/dashboard.yaml:24`, `src/data/personnel.yaml` insetTitle (interim copy "─ Employment Records ─" until Phase E replaces the badge), `src/data/site.yaml` window 2 → `{id: employment, name: employment}` (+ every `id: personnel` route/view reference: `views.ts`, `tmux.ts` ProgramName, `src/pages/personnel.astro` → `employment.astro`, component folder `src/components/personnel/` → `employment/` with Personnel.svelte → EmploymentRecords.svelte or keep inner names consistent — pick ONE convention: folder `employment-records`, component `EmploymentRecords.svelte`, testids `employment-*`), `cmdline.yaml`, `src/content/help/personnel.md` → `employment-records.md` (label/hint), `src/content/notifications/new-personnel-record-added.md` copy, README.md line 16, unit `helpSearch.test.ts`, e2e `personnel.spec.ts` → `employment.spec.ts` (testids + `2:employment` strings + URL regex), visual `recipes.ts`.
- [ ] C3. Regenerate indexes (`pnpm generate` — grep-index/fs-index reflect renamed paths), run full suite, ⟲ rebaseline goldens (expected diffs ONLY: status-bar `2:employment*`, personnel inset title text). Commit(s): collection rename, employment rename, rebaseline (may fold into the rename commits).
- Verify: `git grep -in "personnel files"` → 0 hits outside UI-Mockups/prompt files; `git grep -in "getCollection(\"projects\")"` → 0; `grep -l "content/projects" fixtures/*.json public/generated/*.json` → 0; full suite green; golden diff reviewed — expected regions: status-bar `2:employment*`, personnel inset title, plus grep/shell recipes whose fixture paths legitimately changed (verifier lists which PNGs changed and justifies each).

### Phase D — Repositories page (rename + UI v2 + data) ⟲
- [ ] D1. Rename builds→repositories per Decision 8 (files, ViewId, route, window 1 `{id: repositories, name: repos}`, testids, yaml, help scope `builds.md` → `repositories.md` label "Repositories", e2e spec rename). Suite + ⟲ rebaseline (expected: `1:repos*` in status bar everywhere). Commit.
- [ ] D2. Shared `PanelBadge.svelte` (in `src/components/` root): pill per mockup — border `1px solid rgba(74,159,224,.5)`, radius 3px, bg `rgba(10,14,19,.96)`, shadow `0 0 20px rgba(224,69,60,.18), inset 0 0 14px rgba(74,159,224,.12)`, padding `2px 13px`, font-size 11.5px, letter-spacing .2em, color `#8fd0f5`, glyph `<img src="/assets/spider-glyph-red.svg">` 13×13 with `filter:drop-shadow(0 0 6px rgba(224,69,60,.6))`, wrapper `position:absolute;left:0;right:0;top:0;transform:translateY(-50%);display:flex;justify-content:center;pointer-events:none;z-index:4`. Accent-parameterized (Help teal variant: border `rgba(87,226,201,.5)`, bg `rgba(5,7,10,.92)`, shadow `0 0 14px rgba(87,226,201,.16)`, padding `3px 12px`, font `600 11.5px`, letter-spacing .16em, color `#57e2c9`, glyph spiderman-teal 10×14). Copy the 3 needed SVGs into `public/assets/`.
- [ ] D3. Repositories UI v2 in the refactored `src/components/repositories/` files: renumber panels to reading order 0=Status,1=Repositories,2=Files,3=Content(was Changes),4=Commits,5=Command Log (focusable; extend focusedPanel type + digit keys 0-5 + per-panel key blocks + testids `repositories-panel-N`); badges on all 6 panes (`{n} · {Label}` text); captions moved into panel body (Files/Content: `color:rgba(143,208,245,.6);font-size:11px;border-bottom:1px solid rgba(224,69,60,.14)`, ellipsis truncation; Commits: `main · N commits`, no truncation); mockup per-panel padding verbatim (Files `18px 10px 8px`, Content `18px 14px 9px`, Commits `18px 14px 9px`, Repositories `12px 10px 8px`, Log `12px 14px 10px`, Status `10px 12px 9px`); layout left col `width:31%` (Repositories `flex:1.1`, Files `flex:1.5`), right col `flex:1` (Content `flex:1.95`, Commits `flex:.95`, Log `flex:none`); `pls` keyframe `0%,100%{opacity:.45}50%{opacity:1}` on Status "connected" dot (7px `#5fc6b4`, 2.4s) and active-open-repo row dot (6px `#ffca28`, 1.6s; idle rows static `#4a9fe0`/`#3c78aa`); commits-pane spine graph per Decision 3; Status pane copy per Decision 4. Update e2e for renumbering. Commit.
- [ ] D4. Contribution chart: `scripts/generate.mjs` new step per Context (GraphQL→scrape→stale), `public/generated/contributions.json`, `fixtures/contributions.json` (seeded deterministic, mockup's LCG is fine), Status-pane grid markup (7-row grid, 6px cells, 2px gap, 52 weeks, red palette `["rgba(196,216,232,.07)","rgba(224,69,60,.24)","rgba(224,69,60,.45)","rgba(224,69,60,.68)","#e0453c"]`), loaded like other generated JSON (via shellIndex-style fetch or build-time import — follow how repos/*.json load). Run generation for real. Commit.
- [ ] D5. Command log → content collection per Decision 5: `src/content.config.ts` schema, `src/content/command-log/command-log.md`, build-time mapper to `CommandLogLine[]`, remove `commandLog` from `repositories.yaml`, wire through `repositories.astro`. Commit.
- [ ] D6. Suite + ⟲ rebaseline (expected: repositories page recipes + status bar). Verifier compares rendered values against D2/D3 CSS lists (playwright `getComputedStyle` spot checks or DOM dump on the built page for: badge border/shadow/letter-spacing, panel paddings, grid cell size/colors, dot animations present with correct durations, pane numbering text `0 · Status`..`5 · Command Log`).
- Verify (whole phase): full suite green; `git grep -n "builds"` in src/ → only historical comments if any (target 0 in identifiers/copy); contribution JSON exists with 364+ day entries and real dates (this year), levels 0-4; command log renders from the .md (delete a line in a scratch build to prove sourcing — or verifier greps built dist for a log string present in md and absent from yaml).

### Phase E — Employment Records page v2 ⟲
Rebuild per mockup + Personnel-Panel-Changes.md in `src/components/employment-records/` (post-B3/C2 structure):
- [ ] E1. Flat 6-row list keyed to `personnel` collection entries newest-first (grid cols `12px 12px minmax(0,1fr) 30px 62px 32px`: cursor › / file icon / name / org tag / `.rw-r--r--` / size); drop tree/`../`/filter code paths; j/k/enter per Decision 7. Top-straddling PanelBadge "Employment Records" on the left panel and "File Preview" on the right (blue/red variant, split-text style `Employment <glyph> Records` matching mockup); remove bottom prompt row + hint bar; both panels `flex:1;min-width:0`.
- [ ] E2. Index block under badge, above breadcrumb: `── index ──` rule + `orgs: N` / `longest: N mo` / `years active: YYYY – YYYY`, label `rgba(196,216,232,.32)`, values teal `rgba(95,198,180,.72)` (orgs/longest) and gold `rgba(217,176,74,.75)` (years) — all three DERIVED from the real collection (org count, max tenure months, min-start–max-end years). Breadcrumb `~/employment-records` in `#5fc6b4` + dotted rule. Skip the mockup's `sort mtime desc · filter *.md · hidden 0` meta line (mock-only decoration) unless it's trivially true — record either way.
- [ ] E3. Timeline column (`width:188px;flex:none;margin:0 10px`): spiderweb `<img src="/assets/spider-web-red.svg">` top:2px 112×108 in a 54px-clipped container at opacity .55 — FIRST verify the SVG's intrinsic fill is `#e0453c`; if not, adjust so web visually equals `rgba(224,69,60,.55)` exactly (user's explicit ask: web and spine must look connected); spine `left:50%;top:56px;bottom:16px;width:1px;background:rgba(224,69,60,.55)` (starts at web center); spark overlay `spark 4.6s linear infinite`; selected glow 78×78 with `transition:top .45s cubic-bezier(.2,.75,.2,1)`; flanking dashed connectors `background-size:24px 1px; animation:dash .9s linear infinite` (one reverse); nodes: outer dashed ring `spin` 9s selected/24s idle, inner solid `rspin` 14s; sizes 54/40 selected vs 30/24 idle; dot 30px selected (radial `#8fe3ff→#4a9fe0 55%→#e0453c`, glow `0 0 18px rgba(224,69,60,.55),0 0 34px rgba(74,159,224,.28)`) vs 19px idle flat `rgba(11,15,20,.85)`; node number = `count - i`; size/color transitions .3-.4s. Do NOT port dead keyframes (pls/blink/webglow). All infinite animations disabled under fixtureMode (site convention). Corner sigils per Decision 10 (check mockup data-props first).
- [ ] E4. Write `docs/changes/employment-records-v2.md`: everything changed vs the previous in-repo version (drill-down→flat list, removed bars, badge move, timeline addition, animation inventory BEFORE (just the `blk` filter cursor) and AFTER (spin/rspin/spark/dash + transitions), rename). Update `tests/e2e/employment.spec.ts` for the new model (many of the 59 tests cover drill-down/filter — rewrite to cover flat list, selection↔timeline sync, index block values, removed bars) and `personnel.yaml` → prune dead copy.
- [ ] E5. Suite + ⟲ rebaseline. Commits: rebuild, tests, doc.
- Verify: full suite; spine/web color match checked via the SVG's intrinsic fill attribute + applied CSS opacity (or pixel sampling with small per-channel tolerance — NOT exact pixel equality against an anti-aliased SVG edge), badge at top (not bottom), equal panel widths (bounding boxes within 1px), index values match collection-derived truth (verifier recomputes from `src/content/personnel/*.md`), no `f`-filter remnants (`git grep filterQuery src/components/employment-records` → 0). Phase E also builds in the target component pattern per Decision 12 (verifier checks folder structure: orchestrator + state class + panel children).

### Phase F — Help page v2 ⟲
- [ ] F1. Teal pill section headers per D2 variant + `<img src="/assets/spiderman-teal.svg">` 10×14 replacing the CSS-mask glyph; dotted rule + hint unchanged.
- [ ] F2. Remove ALL caps: delete `text-transform:uppercase` at HelpView.svelte 126/190/222/242 (post-refactor paths will differ — grep), literal `SCOPES`→`Scopes`.
- [ ] F3. Copy updates in `src/content/help/`: repositories.md (rows describe `[0] Status, [1] Repositories, [2] Files, [3] Content, [4] Commits, [5] Command Log`), tmux.md window-jump row (`1:repos 2:employment ...`), scope label check across all 14 files for stray caps/stale names.
- [ ] F4. Suite + ⟲ rebaseline (help recipes). Commit(s).
- Verify: `git grep -n "text-transform:\s*uppercase" src/components` → 0 for help files; built help page DOM contains no ALL-CAPS scope/row labels (verifier greps rendered text); pill computed styles match teal spec; help copy matches final panel numbers exactly.

### Phase G — Notifications finishing
- [ ] G1. Re-circulation per Decision 6 in `notificationStore.ts` + unit tests (exhausted pool re-injects oldest archived non-spam; spam never resurrects; injected item unread + toast-spawned; stable across reload).
- [ ] G2. New visual recipe: notifications panel OPEN on dashboard (fixture mode, animations off) → new golden pair. Optionally a toast recipe if deterministic under fixture seed — only if `E2E_NOTIFICATIONS_INJECT_SEED` hook makes it stable; otherwise skip (e2e already covers toasts).
- [ ] G3. Suite green (new goldens are additions, not rebaselines). Commit(s).
- Verify: unit tests for recirculation pass and actually assert the rule (verifier reads them); e2e notifications suite green; new golden exists in both viewports.

### Phase H — Dashboard check
- [ ] H1. After C2, verify dashboard matches mockup: menu label "Employment Records" + `C-b 2` hint, `2:employment` tab, everything else unchanged. Expected NO code change; if diff found, fix and rebaseline the dashboard recipe only.
- Verify: e2e dashboard.spec.ts (from A2, updated in C2) green; golden 01-dashboard matches post-C baseline.

### Phase I — Docs, README, agent files
- [ ] I1. Screenshots: real (non-fixture) build + preview; capture 1512×945 PNGs via a small Playwright script (reuse `tests/visual/pipeline.mjs` captureState against the real build) into `docs/screenshots/`: home/dashboard, repositories, employment-records, profile, help, notifications-panel-open, plus terminal surfaces (shell session, editor, grep overlay, session picker/choose-tree, copy mode, boot). Script goes in `scripts/` (kept, it's a real tool).
- [ ] I2. `docs/project-structure.md` (repo layout incl. content/data/generated convention + component folder pattern), `docs/architecture.md` (stack, coding standards incl. Svelte 5 runes idioms + testing gates + data-flow reasons), `docs/how-to-run.md` (pnpm install/generate/dev/build/test matrix, GITHUB_TOKEN role, fixtures).
- [ ] I3. README.md rewrite: title `SpideyHub`; description (tmux-emulated personal portfolio); hero = dashboard screenshot; Key Features as 3 tables — `Absolute Web-Slinger!` / `thwip-tastic` / `Solid` (rank features by cool factor: tmux emulation w/ prefix keys, visual golden pipeline, generate-time Shiki highlighting, live GitHub commits+contributions, notification system, vim-lite editor, in-browser shell, grep overlay, boot sequence, Material icons, content collections, fixture mode...); Accomplishments — first-person, XYZ-format-ish but natural, resume-grade claims backed by the repo (e.g. golden pipeline count, 421 e2e tests, zero-runtime-highlighting); Key Pages — section per page (Dashboard, Notification Inbox, Repositories, Employment Records, Profile, Help, plus each terminal surface) with description + screenshot.
- [ ] I4. `CLAUDE.md` + `AGENTS.md`: point to docs/ files as canonical; CLAUDE.md includes the rule "when docs/ change, update AGENT_CHECKLIST.md". `AGENT_CHECKLIST.md`: per-change checklist (check/test gates, goldens policy, naming conventions, data-vs-content placement, commit style) — fold in `.claude/checklist.md` content and delete that file if fully superseded. Check for existing root CLAUDE.md/AGENTS.md first and MERGE, never overwrite.
- [ ] I5. Commit(s). Verify: files exist, README images render (paths valid, files non-empty), no stale names (grep "Personnel Files"/"builds" in new docs → 0), how-to-run commands actually work (verifier dry-runs `pnpm check`), screenshots show post-v2 UI (verifier eyeballs 2-3).

### Phase J — Cleanup + rename (LAST; destructive)
- [ ] J1. Add `UI-Mockups/` to `.gitignore`. Commit.
- [ ] J2. Delete `prompt.md`, `notifications-prompt.md`, `refactor-components-prompt.md`, `PLAN.md` (git rm for tracked PLAN.md; plain rm for untracked). Commit (`Remove iteration scratch files`).
- [ ] J3. Final full suite + `pnpm build` (real) green. Record final commit sha.
- [ ] J4. Write the iteration-6 memory file + any other durable artifacts BEFORE the rename (post-mv, old-cwd tool calls can fail).
- [ ] J5. Rename folder: `mv /Users/shev/Development/perosnal-portfolio-v3 /Users/shev/Development/SpideyHub` — ONLY as the very final action (breaks session cwd; nothing may run after except the completion report, which must note the new path).

## Acceptance criteria (stop conditions)

1. All mockup-derived UI values verified on the built site for Repositories/Employment/Help/Dashboard (verifier computed-style/DOM checks listed per phase).
2. `pnpm check`, `test:unit`, `test:e2e`, `test:visual` all green at the final sha; goldens changed only in phases marked ⟲ and only in expected regions; Phase B changed zero goldens.
3. `git grep` clean for: "Personnel Files", `getCollection("projects")`, builds identifiers/copy (outside gitignored mockups and git history).
4. Contribution grid renders 52 weeks of real data (or documented stale-snapshot fallback if both fetch paths fail — record which path ran).
5. Docs + README + CLAUDE/AGENTS/AGENT_CHECKLIST exist per I; screenshots current.
6. Working tree clean, prompt/PLAN files gone, UI-Mockups ignored, folder renamed.
7. Out of scope (do NOT expand): deploy, resume content, Fonts-API/a11y-CI residuals from iteration 5, restructuring data/generated folders.
